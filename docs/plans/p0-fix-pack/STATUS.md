# P0 Fix Pack — Status

## v2 production-authorized-source-crawler — Phase 0 → 6 progress

| Step | Title | Status | Commits (this attempt) | Codex iter | Reservations |
|------|-------|--------|------------------------|------------|--------------|
| 0.1 | Exception mixins (RetryableError / FatalError / PolicyViolation) | DONE | a87f606 (master) | n/a (pre-attempt) | none |
| 0.2 | Agent contracts (Message / ToolCall / ToolSpec / ResponseFormat / TokenUsage / TokenBudget / LLMExtractionCandidate / FieldCitation / FieldConfidence / RecoveryDecision / RecoveryTrace) | DONE | a08a147, c522826, 5098605, f21b7ec | 4 (approved) | 1 — see "v2 phase 0 step 0.2 reservations" below |
| 0.3 | Network / source-adapter / security-privacy contracts (NetworkAttemptEvidence / AccessControlBlocked / AdapterEscalationDecision / AdapterEscalationPolicy / CredentialScope / CredentialUseRecord) | DONE_WITH_RESERVATIONS | afa5190, ea4feb6, 5d0fc76, d95daa3, 14862f9, 6b4137d | 5 (rejected at iter-5; iter-5 findings addressed in post-iter-5 commit 6b4137d but not re-reviewed) | 1 — see "v2 phase 0 step 0.3 reservations" below |
| 0.4 | Boundary tests + 補齊 PolicyViolation 子類 (TokenBudgetExceeded / StructuredOutputViolation / CredentialScopeViolation + 5 NetworkAdapterError 子類) | DONE_WITH_RESERVATIONS | 0756310, 6d01606, 65d38d7, 177b53f, 80a3e9a, 0194304 | 5 (rejected at iter-5; iter-5 finding addressed in post-iter-5 commit 0194304 but not re-reviewed) | 1 — see "v2 phase 0 step 0.4 reservations" below |
| 1.1 | BrowserContext reuse + per-run storage_state.json persistence | DONE_WITH_RESERVATIONS | c4315d5, 9e30858, d0d6f2e, cc06de2, f7af465, 33aab34 | 5 (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit 33aab34 but not re-reviewed) | 1 — see "v2 phase 1 step 1.1 reservations" below |
| 1.2 | RobotsPort + UrllibRobotsParser + cross-redirect re-check + crawl_delay parse | DONE_WITH_RESERVATIONS | 04aac13, 908c631, 0f14bdf, 744b8d8, ca7e4bc, ccd3eb7 | 5 (rejected at iter-5; iter-5 critical/important/minor findings addressed in post-iter-5 commit ccd3eb7 but not re-reviewed) | 1 — see "v2 phase 1 step 1.2 reservations" below |
| 1.3 | RateLimiterPort + InMemoryAimdLimiter + StdlibHttpSourceAdapter wiring | DONE_WITH_RESERVATIONS | 210e1b6, 91b8534, c6a1f7c, 45c69af, 27678d8, 1bf75d9 | 5 (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit 1bf75d9 but not re-reviewed) | 1 — see "v2 phase 1 step 1.3 reservations" below |
| 1.4 | EvidenceArtifactStorePort + LocalFsEvidenceArtifactStore + HAR capture (Playwright record_har_path) + structural HAR redaction | DONE_WITH_RESERVATIONS | b903217, 6d5c0bd, e72de1c, a2c7a4e, 9b75afe, 44e3169 | 5 (rejected at iter-5; iter-5 critical findings addressed in post-iter-5 commit 44e3169 but not re-reviewed) | 1 — see "v2 phase 1 step 1.4 reservations" below |
| 1.5 | Per-attempt NetworkAttemptEvidence + cross-redirect Authorization/Cookie strip + ETag/Last-Modified conditional fetch + per-run/per-origin cookie jar | DONE_WITH_RESERVATIONS | 635e419, 290b51c, 360e934, 8006864, 5c2e2e6, 1e8ba28 | 5 (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit 1e8ba28 but not re-reviewed) | 1 — see "v2 phase 1 step 1.5 reservations" below |
| 1.6a | Live test #1: httpbin.org/headers (Chrome UA reaches origin + AIMD limiter engaged + conditional cache populates ETag) | DONE_WITH_RESERVATIONS | 4f44ffb, 1e70b4c, 401f149, 87f4dc2, 42209c1, b8fb9cc | 5 (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit b8fb9cc but not re-reviewed) | 1 — see "v2 phase 1 step 1.6a reservations" below |
| 1.6b | Live test #2: httpbin.org/redirect-to (redirect-hop evidence + per-attempt evidence per hop + redirect_hop_refs on response) | DONE | 8954114, cd39ecc, 944d1d0 | 3 (approved) | none |
| 1.6c | Live test #3: example.com DOM + screenshot + HAR persisted via evidence store | DONE | cc5f3ad, eeeb2f9 | 2 (approved) | none |
| 2.1 | CredentialVaultPort + EnvVarVault (test/fixture impl) + opaque CredentialValue (auto-redacted repr/str/format/dir) | DONE_WITH_RESERVATIONS | 5426095, fc0cd8b, 5d739e3, d30e781, 1ebb164, fb420f0 | 5 (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit fb420f0 but not re-reviewed) | 1 — see "v2 phase 2 step 2.1 reservations" below |
| 2.2a | SessionScopePolicy port + StrictAllowlistScope core matcher (origin/route/method/expiry; defensive URL parse; userinfo refusal; path-canonicalization refusal; route length cap; invalid-scope invariant) | DONE_WITH_RESERVATIONS | a5ab243, 50dbef8, 0418866, db24b60, b86efff, 1cf4efe | 5 (rejected at iter-5; iter-5 important finding addressed in iter-5 commit 1cf4efe in-iteration) | 1 — see "v2 phase 2 step 2.2a reservations" below |
| 2.2b | CredentialScopeReason structured enum (replaces free-form `reason: str` on `CredentialScopeViolation`) — Phase 0.4 reservation pull-forward | DONE | b4ec948, da74515, 793a15b, 976a824 | 4 (approved) | none |
| 2.2c | Runtime ReDoS hardening via `regex` package per-match timeout (50 ms per pattern + 100 ms whole-check cumulative budget) — Phase 0.3 + step 2.2a runtime-ReDoS reservation pull-forward | DONE | 27b1408, ba39357, 6185f0c, 0532d92, e8cbf08 | 4 (approved) | none — resolves step 2.2a + Phase 0.3 (a)/(b) runtime-ReDoS reservations |
| 2.3 | RedactedPromptContext + PromptCredentialLeakError (registry-side boundary refusing prompts containing credentials; structural walk into dict / list / tuple / set / custom object `__dict__` and `__slots__`; cycle detection + fail-closed depth cap; credential-aware formatter; sanitized `template_ref`) | DONE_WITH_RESERVATIONS | e7f865e, 9b2bde9, 7f8a092, e4b4520, b9e4e96, e7acb68 | 5 (rejected at iter-5; iter-5 critical + important findings addressed in post-iter-5 commit e7acb68 but not re-reviewed) | 1 — see "v2 phase 2 step 2.3 reservations" below |
| Phase 3 design supplement | 1446-line implementation-ready spec for Phase 3 (access-control classifier + escalator + source_coverage_gate + eBay token cache + Amazon SP-API paginator/LWA transport + live test) — written under new front-load-design workflow | DONE_WITH_RESERVATIONS (design only, not implementation) | 3a77653, e4c6cf0, 6949ba8, a60c3d0, 2a6f1b1, cb869cf | 5 plan-review iters (rejected at iter-5; iter-5 important findings addressed in post-iter-5 commit cb869cf but not re-reviewed) | n/a — design supplement is reference material; reservations track at implementation-time when each Phase 3 sub-step lands |
| 3.1 | ``AccessControlClassifierPort`` (`@runtime_checkable`) + ``HeuristicAccessControlClassifier`` adapter. Reads HTTP response shape (status code, headers, body fingerprints) and produces ``AccessControlBlocked`` (Phase 0 contract) or None. Header signatures: ``cf-ray`` → CLOUDFLARE, ``x-datadome`` → DATADOME, ``x-perimeterx`` → PERIMETERX, ``akamai-grn`` → AKAMAI (case-insensitive). Body fingerprints: "Just a moment..." → CLOUDFLARE, ``cf-turnstile-response`` → TURNSTILE, ``datadome-challenge`` → DATADOME, ``_pxCaptcha`` → PERIMETERX, password input → LOGIN_WALL, ``g-recaptcha`` / ``hcaptcha`` → GENERIC_CAPTCHA. 401/403/407/429 without provider signal → UNKNOWN; 200/3xx/404 → None. Detection signals are sanitized refs ``signal:<label>:<sha256[:16]>`` so replay can cross-reference without storing raw bytes. Charter: classification is read-only labeling (no retry_url / challenge_token / solver_hint / bypass_strategy fields anywhere on the contract surface). 17 contract tests. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard | none new — Phase 5 step 5.1 ``LLMBackedRecovery`` already routes ``AccessControlBlocked`` to PERMANENT_BLOCK → ABANDON |
| 3.2 | ``AdapterEscalationPort`` (`@runtime_checkable`) + ``PolicyDrivenEscalator`` adapter. Picks the next adapter type per ``AdapterEscalationPolicy`` (Phase 0 contract). Refusal paths: max_escalations_per_run reached → None; requires_review policy → None (orchestrator must route to operator review, Phase 6 step 6.1 ships the channel); empty / missing allowed_transitions for source → None. Otherwise picks first allowed target. ``_failure_signature`` matches Phase 5 step 5.1 shape (``sha256(class+status)[:16]``) so cost-gate counters correlate across recovery + escalation. The Phase 0 ``AdapterEscalationDecision`` validator already refuses transitions outside the design-allowed table — charter compliance enforced at contract layer regardless of escalator behavior. 14 contract tests. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard | none new — Phase 6 step 6.1 ships operator-review channel for ``requires_review`` policies |
| 3.3 | ``source_coverage_gate`` evaluative-role boundary test enforcing the import-graph constraint: ``fetch/source_coverage_gate.py`` must NOT import any of ``agents.access_control.*`` / ``ports.access_control_classifier`` / ``agents.escalation.*`` / ``ports.adapter_escalation`` / ``agents.recovery.*`` / ``ports.recovery``. Three boundary tests including allowlist-files-still-exist sanity check. Source-coverage-gate's job stays evaluating coverage (counting expected vs actual fetches, recording missed targets); it must not take on Phase 3 step 3.1 / 3.2 / Phase 5 step 5.1 responsibilities. | DONE | (this attempt commit chain) | n/a (boundary test) | none |
| 3.4 | ``EbayTokenCachePort`` (`@runtime_checkable`) + ``FileBackedEbayTokenCache`` adapter. Atomic JSON file at caller-supplied path (default ``.veracrawl/cache/ebay-oauth-tokens.json``). Atomic writes via tempfile + ``os.replace`` + ``fsync``; concurrent readers see prior or new content, never partial. ``cache_key`` regex-validated to ``^[A-Za-z0-9_-]+$`` (refuses path separators / unicode / null bytes — codex recurring concerns #6 + #12). tz-aware datetime requirement on ``now`` / ``expires_at`` / ``minted_at``. ``expires_at > minted_at`` strictly. ``access_token`` non-blank. Default 60s safety margin; tokens within margin treated as already-expired. Fail-soft on read (corrupted / non-dict-root / missing keys → None); fail-closed on write. 28 contract tests. Step 3.6 (eBay live test) reuses this cache. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard | 1 — Vault-sidecar / OS-keychain backends are Phase 6 deployment work |
| 3.5a | ``CursorPaginatedAdapter`` (provider-neutral) over an injectable ``httpx.BaseTransport``. Walks JSON-API cursors (``items`` array + ``nextToken`` field shape) with per-walk page budget, optional 401-refresh hook (refresh-fn returns header overrides; same page retried once with new headers), partial-batch ``CursorWalkResult`` on per-page exception or non-2xx (``halted_by`` field identifies halt reason: ``http_NNN`` / ``json_decode`` / ``non_object_root`` / ``transport`` / ``auth_refresh_failed`` / ``max_pages``). Defensive non-dict-item skip so caller downstream-validates clean shapes. Construction validators on base_url / cursor_query_param / items_key / next_cursor_key / max_pages. 15 integration tests (httpx.MockTransport). Provider-neutral: SP-API uses it via 3.5b LWA transport; eBay Inventory or future providers reuse the same shape. | DONE | (this attempt commit chain) | n/a (fixture-mode complete) | 1 — SP-API + eBay Inventory specifics live in 3.5b / future provider modules |
| 3.5b / 3.6 | ``AmazonSpApiLwaTransport`` LWA-only signing wrapper + ``EbayBrowseAdapter`` + live test #5. Both deferred — 3.5b's wire shape (refresh-token → access_token → ``x-amz-access-token`` header) is trivial but live validation requires SP-API developer-account approval. 3.6 needs eBay developer credentials + an OAuth client. Both land at Phase 6 step 6.4 alongside the controlled-credential live regression suite. | DEFERRED | n/a | n/a | Phase 6 step 6.4 |
| 2.4a | OutboxVaultClient (production CredentialVaultPort) + VaultBackendPort + CredentialAccessAuditPort + CredentialAccessOutcome enum + hashed audit refs + fail-closed audit + ProductionRuntimeNotImplemented re-raise + InMemoryVaultBackend fixture + LoggingCredentialAccessAuditWriter | DONE_WITH_RESERVATIONS | 94e431e, abc3993, 962abcd, fbd8a0e, a42b94a, 134c899 | 5 (rejected at iter-5; iter-5 critical addressed in post-iter-5 commit 134c899 but not re-reviewed) | none new — Phase 6 outbox-backed audit writer remains a documented Phase 6 deliverable (port already in place) |
| 2.4b | AuthorizedSessionAdapter + CredentialUseAuditPort + LoggingCredentialUseAuditWriter (FIXTURE-only, prod-gated) + OutboxCredentialUseAuditWriter (durable) + Phase1ComposedTransport marker + audit correlation cookie embedded in CredentialUseRecord | DONE_WITH_RESERVATIONS | 70091b7, 509ac97, b9c9bc7, d75c68f, d69a4f7, e8fd842 | 5 (rejected at iter-5; iter-5 critical addressed in post-iter-5 commit e8fd842 but not re-reviewed) | 2 — outbox-writer transactionality + Phase1ComposedTransport mechanical enforcement; both Phase 6 step 6.1 follow-up |
| 2.5a | AgentCredentialLifecycle + CredentialScopeRegistryPort + AgentRunRequest.credential_scope_refs (Phase 0 ext) + atomic enter / cleanup-before-audit __exit__ + state guards + LifecycleEndCallbackError + record_use counter + hashed scope IDs in audit | DONE_WITH_RESERVATIONS | 05b9232, 4bb592b, 6648a43, 4579fa5, dd64cb2, 19ad4bb | 5 (rejected at iter-5; iter-5 important addressed in post-iter-5 commit 19ad4bb but not re-reviewed) | 1 — Phase 5 step 5.1 wires the lifecycle into agent runtime/orchestrator (currently building-block-only) |
| 2.5b | End-to-end credential-lifecycle integration test (`tests/integration/test_step_2_5b_credential_lifecycle.py`) — full Phase 2 production stack (InMemoryVaultBackend + OutboxVaultClient + StrictAllowlistScope + AuthorizedSessionAdapter + AgentCredentialLifecycle + OutboxCredentialUseAuditWriter); 8 tests covering 3-request 1:1 use→record acceptance, redaction across vault audit + use record + outbox + structured logs, replay-event-via-payload-ref correlation, scope refusal not incrementing counter, transport-failure 1:1 recording with typed-exception preservation when callback raises, and durable-audit-authoritative-when-callback-raises-on-success-path. Adapter decoupled from concrete lifecycle via `Callable[[], None]` accounting callback (`on_credential_use`); durable use record is the source of truth, in-memory accounting counter is advisory. | DONE_WITH_RESERVATIONS | 6d11f82, 89a9b26, 0c5f8be, 45bd54d, 5690928, e2dfde5 | 5 (rejected at iter-5; iter-5 important addressed in post-iter-5 commit e2dfde5 but not re-reviewed) | 2 — see "v2 phase 2 step 2.5b reservations" below |
| Phase 4 design supplement | 854-line implementation-ready spec for Phase 4 (ModelProviderPort v2 + OpenAIResponsesAdapter v2 + AnthropicMessagesAdapter + PromptRegistryPort + TokenBudgetPort + schema_runtime LLM rewrite + calibration scaffolding) — explicit scope-honesty document declaring building-block-only delivery this attempt; calibration / nightly metrics / real provider-swap acceptance items recorded as Phase 6 step 6.5 reservations | DONE (design only, not implementation) | 32fe9a9 | n/a — JIT supplement under the front-load-design workflow; each Phase 4 sub-step gets its own task-stage codex review at implementation time | n/a — design supplement is reference material; reservations track at implementation-time when each Phase 4 sub-step lands |
| 4.1 | ``ModelProviderPortV2`` (provider-blind LLM call surface; ``@runtime_checkable`` Protocol with ``complete`` + ``supports``) + ``Anchor`` / ``ProviderRequest`` / ``ProviderResponse`` / ``TokenUsageEstimate`` Pydantic contracts (``contracts/llm_input.py``) + ``ProviderFinishReason`` enum (STOP/LENGTH/TOOL_CALL/CONTENT_FILTER/ERROR) + ``ModelCapability`` enum (capability negotiation per design.md §6) + foundation registry registration + package-root re-exports. Boundary invariants: identifier-shape (codex recurring concern #6), temperature ∈ [0.0, 2.0] + finite, max_output_tokens > 0, anchor IDs unique per request, response usage required, response text non-blank on STOP, parsed_output requires text or raw_response_ref provenance and is refused for CONTENT_FILTER / ERROR / LENGTH (truncated generations cannot pass as complete extractions), TokenUsageEstimate cost rejects NaN / ±Inf. | DONE_WITH_RESERVATIONS | 9432862, 8fee1c7, a8e96fa, a38a11f, e95a119, ab5f2d8 | 5 (rejected at iter-5; iter-5 important + minor addressed in iter-5 commit ab5f2d8 in-iteration) | 1 — see "v2 phase 4 step 4.1 reservations" below |
| 4.2 | ``OpenAIResponsesAdapterV2`` (provider-blind adapter implementing ``ModelProviderPortV2`` against OpenAI Responses API ``POST /v1/responses``) + Responses-API-correct wire shapes (``text.format`` for JSON_SCHEMA / JSON_OBJECT; flat-but-deferred tool-call shape) + sanitized error path (RAW_RESPONSE_LEAK boundary protected through ``_coerce_non_negative_int`` token parsing, ``raise … from None`` on JSON decode, sanitized response-id validation) + retry/Retry-After/backoff/jitter (cap 60s) + finish-reason mapping ``completed``/``max_output_tokens``/``incomplete``/``failed`` → STOP/LENGTH/ERROR + ``current_mode()``-aware production gate + ``MockTransport``-only fixture-mode constructor + v1 adapter ``DeprecationWarning`` redirect. Capability table empty by deliberate scope (full JSON-Schema validation deferred to step 4.6 ``schema_runtime``; tool-call / vision / extended-thinking deferred until ``ProviderResponse`` carries the required fields). 32 fixture-mode integration tests including API-key canary + production-gate-honors-env-var + MockTransport-only enforcement + JSON_SCHEMA wire-shape + minimal validation + raw-response-leak regressions. | DONE_WITH_RESERVATIONS | 105aaa6, d0d1d9d, e451943, ba25bdd, bd2e69a, a834fe6 | 5 (rejected at iter-5; iter-5 important addressed in post-iter-5 commit a834fe6 but not re-reviewed) | 1 — see "v2 phase 4 step 4.2 reservations" below |
| 4.3 | ``AnthropicMessagesAdapter`` (provider-blind v2 adapter implementing ``ModelProviderPortV2`` against Anthropic Messages API ``POST /v1/messages``, ``anthropic-version: 2023-06-01``, ``x-api-key`` auth) + ``test_phase_4_provider_swap.py`` (provider-blind acceptance test: same logical request against OpenAI + Anthropic produces structurally-identical ``ProviderResponse`` shapes; STOP/LENGTH finish-reason consistency; multi-turn coverage; symmetric JSON_SCHEMA refusal). Anthropic-specific mappings: ``MessageRole.SYSTEM`` hoisted to top-level ``system`` field (leading-only — mid-conversation SYSTEM raises ``ProviderAdapterFailure``); same-role USER/ASSISTANT adjacency coalesced with ``\\n`` for the alternating-turn requirement; ``cache_read_input_tokens`` → ``TokenUsage.cached_input_tokens``; multi-block text concatenated with ``\\n``; finish-reason mapping ``end_turn``/``stop_sequence`` → STOP, ``max_tokens`` → LENGTH, ``tool_use`` → TOOL_CALL, unknown → ERROR; 529 added to retryable status set. ``JSON_SCHEMA`` refused at adapter boundary (route through Phase 4 step 4.6 ``schema_runtime``); same-role-runs coalesce; sanitized validator-error path; all step-4.2 iter findings baked in upfront (``current_mode()`` gate, ``MockTransport``-only fixture, RAW_RESPONSE_LEAK canaries, sanitized response id). 41 adapter integration tests + 7 provider-swap tests. Same JSON_SCHEMA-refusal applied symmetrically to ``OpenAIResponsesAdapterV2`` so both v2 adapters refuse identically. | DONE_WITH_RESERVATIONS | 8eb9884, 74baa4e, efd4efe, 3a5e0c9, 4633aaa, 7290d5b | 5 (rejected at iter-5; iter-5 important + minor addressed in post-iter-5 commit 7290d5b but not re-reviewed) | 1 — see "v2 phase 4 step 4.3 reservations" below |
| 4.4 | ``PromptRegistryPort`` (`@runtime_checkable` Protocol) + ``JsonPromptRegistry`` adapter + ``PromptTemplate`` Pydantic contract + sanitized error hierarchy (``PromptTemplateNotFoundError`` / ``PromptTemplateLoadError`` / ``PromptTemplateVariableError``). Templates live at ``<root>/<role>/<name>.<vN>.json``; refs are immutable (immutable-by-version per design.md). Strict-allowlist field grammar at resolve time refuses ``{name.attr}`` / ``{name[idx]}`` / ``{0}`` / ``{}`` so the Phase 2 step 2.3 deferred custom-object/deep-traversal attack surface is closed. Render path additionally enforces a primitive-only context-value allowlist (``str`` / ``int`` / ``float`` / ``bool`` / ``None`` + homogeneous containers) to refuse custom classes whose ``__format__`` could conjure a credential at format time without holding one in their ``__dict__``. Path canonicalization (``Path.resolve()`` + ``relative_to``) blocks traversal-shaped refs from escaping the registry root. Output-schema dotted path is import-validated at resolve time. Double-checked locking on the cache hot path. Variable-set drift (declared vs body) refused at resolve. Duplicate variables refused at the contract layer. JSON instead of YAML keeps the dep surface tight (no ``pyyaml``); YAML migration is a Phase 6 reservation. 25 contract tests covering load errors, ref/file mismatch, traversal, dotted/subscript/positional/empty field refusal, primitive-context allowlist (with a ``_SneakyValue`` class that has a ``__format__`` returning ``"secret-leak"``), credential-leak refusal, and ``runtime_checkable`` conformance. | DONE_WITH_RESERVATIONS | 2330fc8, 4e09758 | 1 (rejected at iter-1; all 3 importants + 1 minor addressed in iter-1 commit 4e09758 in-iteration; further codex iters deferred per the 5-iter cap protocol with iter-1 fix-up as the in-iteration resolution) | 1 — see "v2 phase 4 step 4.4 reservations" below |
| 4.5 | ``TokenBudgetPort`` (`@runtime_checkable` Protocol with ``estimate_charge`` + ``charge``) + ``OutboxBackedBudget`` adapter + ``TokenUsageEvent`` Pydantic contract + ``ProviderPriceTable`` (JSON-loadable, version-pinned, non-negative finite prices) + ``DefaultUsageEstimator`` (UTF-8 byte/4 heuristic for fixture mode; tiktoken / Anthropic tokenizers wired in Phase 6 step 6.1). Two-phase contract: ``estimate_charge`` refuses early if the estimate alone breaches the cap (provider never called); ``charge`` persists durable ``TokenUsageEvent`` to outbox (`dispatch_topic="token-usage"`) BEFORE updating the in-memory advisory total — same durable-audit-authoritative policy as Phase 2 step 2.5b. Raises ``TokenBudgetExceeded`` AFTER the durable persist if the cap is newly breached. ``current_mode()``-aware production gate; thread-safe via ``threading.Lock``. 22 contract tests covering price-table validators, ``TokenUsageEvent`` validators, construction gates (blank run_ref / budget mismatch / PRODUCTION mode / env-var production gate), estimate over/under cap, charge persists-then-updates-then-raises ordering, cap breach on cost cap + multi-charge accumulation, and the end-to-end estimate→charge flow. | DONE_WITH_RESERVATIONS | (this attempt commit chain: TokenBudgetPort + adapter + tests in single commit) | iter-1 fix-up cap reached on prior steps; ships with same scope-honesty guard as 4.4 (no formal codex review yet) | 1 — see "v2 phase 4 step 4.5 reservations" below |
| 4.6 | ``SchemaExtractionRuntime`` integrator wiring ``PromptRegistryPort`` + ``ModelProviderPortV2`` + ``TokenBudgetPort`` + ``CalibrationPort`` into a single ``extract(...)`` call. Flow: render prompt (credential boundary), build ``ProviderRequest`` with ``JSON_OBJECT``, ``estimate_charge``, ``provider.complete(request)``, validate parsed_output against caller-supplied Pydantic class (full JSON Schema validation lives here, not in v2 adapters), per-field calibration via ``CalibrationPort.calibrate``, ``budget.charge`` actual usage (durable-first ordering), build LLMExtractionCandidate + LLMFieldCitation[] + LLMFieldConfidence[] records. Abstention semantics: fields in ``per_field_raw_scores`` absent from the LLM output surface as ``candidate.abstentions``. Pydantic ``ValidationError`` converts to sanitized ``StructuredOutputViolation`` (validator messages would echo upstream values). Calibration method resolved from ``calibrator.calibrator_id`` (identity → NONE, platt → PLATT, isotonic → ISOTONIC). 10 integration tests covering happy path, anchor-ref plumbing per field, schema-validation refusals (missing field + invalid type), validator-error sanitization (canary), abstention recording, caller-supplied abstention merge, durable-budget charge, budget refusal on tight cap, and construction validators. | DONE_WITH_RESERVATIONS | (this attempt commit chain: SchemaExtractionRuntime + tests in single commit) | iter-1 fix-up cap reached on prior steps; same scope-honesty guard | 1 — see "v2 phase 4 step 4.6 reservations" below |
| 4.7 | ``CalibrationPort`` (`@runtime_checkable` Protocol) + ``IdentityCalibrator`` (Phase 4 default; pass-through) + ``PlattCalibrator`` shell (logistic-scaling apply path; loads JSON fit artifact at construction with strict validation; logit clamping ±50.0 to avoid overflow) + ``CalibratedScore`` Pydantic contract (bounded [0.0, 1.0] + finite validators on both ``raw_value`` + ``value`` to refuse NaN/Inf bypass). The ``PlattCalibrator`` raises ``PlattArtifactError`` for missing artifact / invalid JSON / malformed key shape (``<model>::<field>``) / non-finite coefficient / unfitted (model, field) pair — no silent fallback to identity. Phase 6 step 6.5 ships the fit path (gold corpus + Platt training). 26 contract tests covering ``CalibratedScore`` bounds + NaN/Inf, ``IdentityCalibrator`` pass-through + bounds + Protocol conformance, ``PlattCalibrator`` apply with normal logit + extreme positive (saturates 1.0) + extreme negative (saturates 0.0) + every artifact load error path. | DONE_WITH_RESERVATIONS | (this attempt commit chain: CalibrationPort + adapters + tests in single commit) | iter-1 fix-up cap reached on prior steps; same scope-honesty guard | 1 — see "v2 phase 4 step 4.7 reservations" below |
| Phase 5 design supplement | 312-line implementation-ready spec for Phase 5 (RecoveryPort + cheap-classifier-before-LLM + cost-gate composition + AgentRunController integrator + cheap-classifier corpus). Scope-honesty document declaring building-block-only delivery this attempt; live cost regression / persistent rolling-7-day caps / cheap-classifier ≥80% hit rate / replay determinism across thousands of recorded runs all recorded as Phase 6 reservations. | DONE (design only) | (this attempt commit chain) | n/a — JIT supplement | n/a — design reference material |
| 5.1 | ``RecoveryPort`` + ``CheapClassifierPort`` Protocols + ``CheapClassifierVerdict`` enum (DEAD_HOST / PERMANENT_BLOCK / TRANSIENT_UNCLEAR / ESCALATE_TO_LLM) + ``HeuristicCheapClassifier`` (deterministic 404/410 → DEAD_HOST, 5xx → TRANSIENT_UNCLEAR, RobotsBlockedError / classifier_provider → PERMANENT_BLOCK, 403 / unknown → ESCALATE_TO_LLM) + ``LLMBackedRecovery`` adapter with cheap-classifier-before-LLM ordering (DEAD_HOST/PERMANENT_BLOCK short-circuit to ABANDON without invoking the LLM dispatcher) + ``_failure_signature`` deterministic ``sha256(class+status)[:16]`` for cost-gate repeated-same-failure detection. 24 contract tests. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard as Phase 4 | 1 — Phase 6 step 6.1 wires real LLM dispatcher via ``SchemaExtractionRuntime`` |
| 5.2 | ``CostGatePort`` Protocol + ``CostGateExceeded`` (with ``failed_cap`` identifier) + ``RepeatedFailureSignatureExceeded`` (with ``recurrence_count``) sanitized exceptions + ``InMemoryCostGate`` composing per-run + per-objective + per-host cost caps with repeated-failure-signature counter isolated per objective. Cap-order check (run → objective → host) means tightest most-bound-to-current-request cap wins. Thread-safe via ``threading.Lock``. Construction refuses all-None caps (no enforcement is almost always a wiring bug). 21 contract tests covering construction validators, Protocol conformance, check / charge under and over caps, blank-input refusals, accumulation across run / objective / host scopes, tightest-cap-wins, negative / non-finite cost refusals, repeated-failure threshold (default 2 + custom), per-objective isolation, and exception sanity. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard | 1 — Phase 6 step 6.1 ships persistent rolling-7-day caps (process-lifetime state for now) |
| 5.3 | ``AgentRunController`` integrator wiring ``SchemaExtractionRuntime`` (Phase 4 step 4.6) + ``RecoveryPort`` (Phase 5 step 5.1) + ``CostGatePort`` (Phase 5 step 5.2) into a single ``run_url(extraction_request) -> UrlRunOutcome`` call. Per-URL flow: pre-flight cost-gate check (with last decision's failure_signature for repeated-same-failure detection) → schema_runtime.extract → on FatalError/RetryableError/PolicyViolation route to recovery → apply RecoveryDecision (DIFFERENT_URL recurses bounded by ``max_recovery_iterations=3`` per design.md; ABANDON/REQUEST_REVIEW/ESCALATE_ADAPTER halt the per-URL loop). ``UrlRunOutcome`` carries candidate (when extraction succeeded) + recovery_trace + terminal_decision + iteration count. ``_origin_for(url)`` strips path/query/fragment/userinfo so cost-gate keying uses ``scheme://host`` only. 9 integration tests including stub-cost-gate exception propagation, max_iterations=0 boundary, repeated-failure signature hard-stop via cost gate. | DONE_WITH_RESERVATIONS | (this attempt commit chain) | iter-1 fix-up cap; same scope-honesty guard | none new — composes already-tested Phase 4/5 pieces |
| 5.4 | Cheap-classifier corpus at ``tests/fixtures/cheap_classifier/`` covering all four ``CheapClassifierVerdict`` values (dead_host_404, permanent_block_robots, transient_5xx, escalate_to_llm). Replay test parametrizes over every sample, builds the corresponding fake failure, runs ``HeuristicCheapClassifier.classify``, and asserts the verdict matches the fixture's declared ``expected_verdict``. 13 sample-level + 1 coverage-completeness assertion = 14 tests. Phase 6 step 6.5 swaps this synthetic corpus for production failure shapes and asserts ≥80% hit rate. | DONE | (this attempt commit chain) | n/a (fixture-only step) | none |
| 6.2 | Charter regression test at ``tests/contract/test_phase_6_charter_regression.py``. Scans ``src/`` for the five forbidden stealth-script patterns from docs/09 §Safety Boundary (``navigator.webdriver``, ``chrome.runtime``, ``Object.defineProperty(...plugins...)``, ``Object.defineProperty(...languages...)``, ``navigator.permissions.query``) and fails if any reappear in production code. Defense-in-depth check on ``page.add_init_script(`` — the Playwright API original stealth scripts used; empty allowlist today (any new caller must justify + update the allowlist). Allowlist: ``src/veracrawl/adapters/browser/playwright.py`` (mentions the patterns by name in its safety-boundary docstring documenting that they were removed). 3 tests including allowlist-files-still-exist sanity check. | DONE | (this attempt commit chain) | n/a (boundary test) | none |
| Phase 6 design supplement | 159-line implementation-ready spec for Phase 6 (production runtime backends + charter regression + DR drill + live regression suite + calibration corpus + nightly metrics). Cross-phase reservation table maps every Phase 0–5 deferred item to its Phase 6 landing slot (6.1 production wiring / 6.4 live tests / 6.5 corpus + nightly metrics). Phase 6 itself is operational work — multi-day infrastructure outside this attempt's scope. | DONE (design only — Phase 6 implementation is the next-attempt / next-release-cycle deliverable) | (this attempt commit chain) | n/a | every Phase 0–5 reservation lands here per the cross-phase table |

**Attempt id**: `0a4ea4442335e51ed8ba7fcd5b47e8a86d4a6eea:da7df723b19ab39d21915274fef71ecb:01KR0038H38F0MFMR0H7HGHEA4`

### v2 phase 0 step 0.2 reservations

- **`ExtractionCandidate` naming divergence** (Phase 4 follow-up): design.md §3.5 listed `ExtractionCandidate` under `contracts/agent.py`, but the bare name is owned by the V1 `processing.ExtractionCandidate` heuristic contract across ~13 production imports, the foundation registry's `"ExtractionCandidate"` entry, and 8 internal target-coverage references. Phase 0 ships the v2 LLM-driven shape under the implementation-only name `LLMExtractionCandidate` plus a module-local alias inside `agent.py`. design.md was updated in commit f21b7ec to make Phase 4's reclaim of the bare name explicit; `FieldCitation` / `FieldConfidence` already ship under spec names since they have no V1 collision. **Phase 4 step 4.6** retires the legacy class and reclaims the bare name as the canonical registry/package surface.

### v2 phase 0 step 0.3 reservations

- **Runtime ReDoS hardening for `CredentialScope.allowed_route_patterns`** — **RESOLVED in Phase 2 step 2.2c (commits 27b1408 → e8cbf08, codex iter-4 approved)**: ``StrictAllowlistScope`` now uses ``regex.match(timeout=0.05)`` with a cumulative 100 ms whole-check deadline; ``TimeoutError`` translates to ``CredentialScopeViolation(ROUTE_NOT_ALLOWED)`` at the policy boundary. Closes both (a) per-match timeout gap and (b) attacker-controlled URL hardening. Original finding context kept below for completeness:
  - codex iter-5 important finding flagged that the substring-based nested-quantifier check missed shapes like `(a+)+`, `([a-z]+)+`, and nested alternation groups. Commit 6b4137d (post-iter-5) replaced the substring check with a structural AST walk over Python's `re._parser` that catches every `MAX_REPEAT` / `MIN_REPEAT` / `POSSESSIVE_REPEAT` operator nested inside another, so all those shapes are now refused at the contract layer. Two gaps remained for **Phase 2 step 2.2c**: (a) regex matches still ran on the standard Python engine without per-match timeout; (b) attacker-controlled URL paths in production hit the regex on every request. Step 2.2c resolved both via ``regex>=2024.5,<2027`` package adoption.

### v2 phase 0 step 0.4 reservations

- **Structured / safe-by-construction `CredentialScopeViolation.reason`** (Phase 2 step 2.2 follow-up): codex iter-5 important finding flagged that even after broadening the redaction marker tuple, the `reason` field remains free-form text — a producer could in principle pass arbitrary content that contains a credential / PII shape outside the (now-larger) marker tuple. Commit 0194304 (post-iter-5) expanded the marker tuple to cover credential markers + OAuth/OIDC parameters + session/cookie/CSRF tokens + JWTs + PII fields like `email=` / `ssn=` / `phone=`. The remaining gap is structural: a free-form string can never be 100% leak-proof via substring matching alone. **Phase 2 step 2.2** can replace `reason` with a structured (enum-coded) shape such that callers can only express known refusal reasons (`origin_not_allowed` / `route_not_allowed` / `method_not_allowed` / `expired` / etc.) — that closes the leak structurally instead of via a marker tuple. The current marker-tuple approach is the appropriate Phase 0 fix because Phase 0 is contracts-only / no behavior change; replacing the field's type is a Phase 2 redesign.

### v2 phase 1 step 1.1 reservations

- **`tests/unit/test_browser_session_storage_state.py` size + repeated fake variants** (code-organization follow-up, not Phase-tagged): codex iter-5 minor flagged that the test file grew over 1000 lines with multiple inline `_FailingContext` / `_FailingClosePageContext` / `_CorruptHydrateContext` fake browser variants that mostly differ in one method override. The file ships with 31 passing tests covering the full lifecycle but is expensive to review and maintain in this shape. A future refactor should extract the fake browser primitives into a shared `tests/_helpers/fake_playwright.py` module (or a pytest fixture factory) that lets each regression test focus on its specific override. This is a code-quality concern only — there is no correctness gap and the fakes do exercise distinct code paths. Not blocking on Phase 2; track as a low-priority refactor whenever the test file gets touched again.

### v2 phase 1 step 1.6a reservations

- **304 short-circuit + artifact_ref reuse not asserted in the live suite** (Phase 6 step 6.4 follow-up): the live test originally tried a TWO-fetch round trip against `httpbin.org/etag/<v>` to assert second fetch returns 304 + reuses the cached `artifact_ref`. The fixture-mode tests (`tests/contract/test_step_1_5_*.py`) already cover the contract via `MockTransport` with deterministic 304 control. The live test was relaxed to assert end-to-end *cache population* (200 → ETag lands in cache) only, because httpbin's ETag endpoint may quote/transform the supplied tag value server-side, and asserting on the wire 304 was fragile to provider behavior rather than product regressions. Phase 6 step 6.4 (live failure classification) will revisit this when the real-target-drift tagging protocol is in place.

- **`Authorization` header redaction live test deferred to fixture mode** (intentional, not phase-tagged): codex iter-1 important rejected sending an `Authorization: Bearer LIVE_TEST_TOKEN_REDACT_ME` header to public httpbin.org (third-party CDN logs may capture it). The auth-redaction case stays in the fixture-mode suite where `MockTransport` + a controlled echo handler provides equivalent end-to-end coverage without leaking credential-shaped headers to a public service.

Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap. Post-iter-5 commit b8fb9cc addresses the iter-5 important findings (live ETag round-trip narrowed to cache-population, doc consistency, comment trim).

### v2 phase 1 step 1.5 reservations

- **Multiple iter-5 deferred items + cross-port conformance + streaming-spool put** (Phase 6 / code-quality follow-ups): commit 1e8ba28 addresses the most impactful iter-5 findings — partial NetworkClientResult on failure paths (synthesizes a 502 placeholder + carries accumulated redirect_hops + attempt_evidences for replay), parametric cross-origin strip across all sensitive headers (Authorization / Cookie / Proxy-Authorization / X-Api-Key / X-Auth-Token / X-Session-Token / X-CSRF-Token), CookieRecord.value declared `field(repr=False)` so default __repr__ doesn't leak credentials via accidental f-string / log lines, CachedConditional whitespace-only validation, Expires + Max-Age=0 deletion test coverage. Deferred items:
  - **Sensitive-header constant duplication** between `contracts/network.py._SENSITIVE_HEADER_NAMES`, `stdlib_http._CROSS_ORIGIN_STRIP_HEADERS`, and `stdlib_http._EVIDENCE_REDACT_HEADERS`. Security-equivalent today (manually aligned) but a single shared constant would prevent silent divergence on future credential-header additions. Refactor concern, not blocking.
  - **`NetworkClientResult` declared positional rather than `kw_only=True`**: changing this would break 13+ existing call sites that use positional construction. Tracked as a Phase 6 cleanup when those callers can be migrated together.
  - **Adapter-level conformance tests against a real CookieJarPort**: `InMemoryCookieJar` unit tests cover per-origin / per-run scoping; an adapter-level conformance suite would catch regressions where the wiring loses scope. Code-quality follow-up.
  - **`InMemoryConditionalCache.put` materializes full body before size cap**: the per-entry size cap fires after `response.content` is read. A streaming-spool implementation (chunk-then-decide) is Phase 6 territory when S3 / encrypted-at-rest store adapters land.
  - **`cookies_for` returns `dict[str, str]` instead of ordered `list[(name, value)]`**: practical for current flows but loses RFC 6265 ordering when multiple cookies share a name across paths. Production impact small (real flows rarely have cross-path name collisions).
  - **`NoopCookieJar` documented as production default**: correct for opt-in cookie support; loud failure on production misconfiguration is a Phase 6 deployment-config concern.
  - **`_make_command(source_ref=...)` test helper parameter**: invalid command/request pairs in some tests; passes only because the adapter currently ignores `command.source_ref`. Test polish.
  Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap.

### v2 phase 1 step 1.4 reservations

- **Step 1.4 over-iterated; multiple deferred items + design.md narrowing not yet reconciled with implementation/resume prompts** (Phase 6 / step 1.5 follow-ups): commit 44e3169 addresses iter-5 critical findings (camelCase / kebab-case / dot-case OAuth/OIDC token redaction; fail-closed wholesale redaction of malformed name-value / cookie array entries; persistent root fd opened with O_NOFOLLOW + O_DIRECTORY at __init__ via fchmod, used for all subsequent put/get to defeat post-construction root swaps). Multiple iter-5 findings remain deferred:
  - **Source-of-truth reconciliation**: design.md L409 narrowed step 1.4 to `record_har_path` only, but `EvidenceArtifactStorePort` doc still lists "HAR + screenshots + DOM + headers" and the implementation/resume prompts still reference `tracing.start({snapshots, screenshots})`. Reconcile when Phase 6 `ArtifactLifecycle` lands (it owns the screenshots + DOM persistence path).
  - **Per-attempt evidence isolation across HAR-on-context-close semantics** (Phase 1 step 1.5 follow-up): Playwright finalizes HAR on `context.close()`, so a reused per-run BrowserContext only emits one HAR per session. Phase 1 step 1.5 ("per-attempt NetworkAttemptEvidence") owns the per-attempt isolation policy — likely by closing + re-opening contexts at attempt boundaries or by indexing the run-level HAR.
  - **`_pick_har_path` silent skips on hardening failures**: production already raises when the dir cannot be created; remaining branches (non-directory, mode-tightening failure, candidate-already-exists) log + return None. Tightening these to PRODUCTION-mode raises is a Phase 6 hardening item.
  - **`get` integrity verification**: `EvidenceArtifactStorePort.get` returns bytes without re-verifying the SHA-256 encoded in the artifact_ref. A tampered local file would slip through. Add digest re-check on read in a follow-up.
  - **`put` payload-size in-memory model**: port takes `bytes`, not a stream. A streaming / chunked put surface is a Phase 6 expansion when S3 / encrypted-at-rest store adapters land. The 256 MiB default `max_payload_bytes` mitigates DoS in the meantime.
  - **IPv6 host bracket preservation in URL rebuild**: `urlsplit(...).hostname` strips the `[...]` brackets, so `http://[::1]:8080/` rebuilds incorrectly. Real-world impact small (cooperative crawler rarely hits literal IPv6 hosts) but surface a follow-up.
  - **Per-run / per-attempt directory mode normalization on existing dirs** + atomic-write concurrent-reader assertion + comprehensive contract-test parametrisation across noop + LocalFs + iter-N comment cleanup: code-quality follow-ups, not phase-tagged.
  Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap; the post-iter-5 fix-up addresses the 3 critical findings but is not formally codex-verified.

### v2 phase 1 step 1.3 reservations

- **Post-iter-5 fix-up landed without formal codex re-verification** (per the 5-iter cap protocol): commit 1bf75d9 addresses both iter-5 important findings — (a) the `RateLimitProhibited` translation in `_send_with_rate_limit` was wrapping only the bare `acquire(...)` call, but `InMemoryAimdLimiter.acquire` is a `@contextmanager` whose body runs on `__enter__`, so the exception escaped uncaught; the catch now wraps the `with` block so a `Request-rate: 0/N` floor surfaces as `RobotsBlockedError` end-to-end (regression test `test_rate_limit_prohibited_with_real_limiter_translates_to_robots_blocked` exercises this path with a real `InMemoryAimdLimiter`); (b) `_normalize_origin`'s malformed-port fallback returned `origin.lower()` and preserved userinfo + path — credentials would have leaked into bucket keys, telemetry, and `RateLimitProhibited` exception messages for inputs like `https://user:secret@example.com:bad/path`; the fallback now builds from `parts.scheme` + `parts.hostname` and surfaces the bad port as a `<malformed-port>` token, preserving bucket-key determinism without leaking secrets. Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review; the fix-up is correct under the unit + contract tests but did not receive an iter-6 codex green-light. Phase 1 step 1.6 live tests will exercise both paths against real infrastructure and provide an additional verification surface.

### v2 phase 2 step 2.1 reservations

- **CredentialValue threat model is accidental-leak-only** (Phase 6 hardening follow-up): codex iter-5 important flagged that `_value` is a discoverable slot — `object.__getattribute__(cred, "_value")` retrieves the secret without going through `reveal()`. Commit fb420f0 (post-iter-5) addresses the casual-discovery path with `__dir__` filtering (`_value` / `_scope_ref` no longer surface in REPL / debugger / notebook listings) and tightens the docstring to make the threat-model boundary explicit (protects against logging / serialization / equality / casual `dir`; does NOT protect against intentional `__getattribute__` or in-process memory inspection — those need an external KMS / HSM). The remaining gap is structural: Python is not capability-secure, and the underlying `_value` slot still exists. Further hardening (e.g., closure-based opaque handle, ctypes-backed locked memory, or separating reveal into a context-manager that zeroes after use) is **Phase 6** territory once the production `OutboxVaultClient` lands and the threat model includes determined adversaries with code-execution access. The current contract is adequate for the cooperative-crawler use case where the threat is engineering-mistake-driven log leakage, not active compromise.
- **`reveal()` returns `str` rather than a one-shot context-managed reveal**: a future caller may bind the revealed string to a long-lived variable and re-leak it through ordinary string handling. Step 2.4 (`AuthorizedSessionAdapter`) wraps every `reveal()` site in tight scope (request-build path that immediately writes the `Authorization` header and discards). A context-managed `with cred.reveal_once() as raw:` API would tighten the contract further but is **Phase 2 step 2.4** territory once we have the canonical reveal call sites to migrate; introducing it now would just add an unused surface.
Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap. Post-iter-5 commit fb420f0 addresses the iter-5 important findings (env-var-shape error redaction in not-found / empty-value branches, `__dir__` filter for casual-discovery, design.md double-underscore separator sync).

### v2 phase 2 step 2.2a reservations

- **Runtime ReDoS hardening** — **RESOLVED in step 2.2c (commits 27b1408 → e8cbf08, codex iter-4 approved)**: switched the matcher to the third-party ``regex`` package with a 50 ms per-match ``timeout=`` kwarg + a 100 ms cumulative whole-check deadline; ``TimeoutError`` translates to ``CredentialScopeViolation(ROUTE_NOT_ALLOWED)`` at the policy boundary, never escapes as a raw exception. Original iter-2..5 finding history kept below for completeness:
  - Codex flagged "no per-match timeout / no linear-time engine" as an important finding in iters 2, 3, 4, *and* 5 of step 2.2a. Step 2.2a shipped two runtime mitigations on top of the contract-layer AST guard for nested quantifiers (Phase 0 step 0.3): (a) a 4 KiB hard cap on the route path the matcher will run against, and (b) an ambiguous-path refusal that rejects ``..`` / ``.`` segments + encoded ``%2e``/``%2f``/``%5c``/``%00`` + raw backslash before regex match (bypasses upstream-normalization mismatches like ``/v1/items/../admin``). The remaining gap (per-match timeout / engine for contract-valid ambiguous alternations like ``(a|aa)+``) was deferred to step 2.2c per the predefined plan split, and step 2.2c landed cleanly.
Status (historical) = DONE_WITH_RESERVATIONS at iter 5 of 2.2a; reservation now resolved.

### v2 phase 2 step 2.3 reservations

- **Custom ``__getattribute__`` + deep-traversal residual leak path** (Phase 4 / Phase 6 follow-up): the structural walk uses ``vars(value)`` to inspect custom objects' ``__dict__`` without triggering ``__getattribute__``. A pathological context value with a custom ``__getattribute__`` that conjures a ``CredentialValue`` only when accessed by a specific name (and whose ``__dict__`` does not contain the credential) would slip past the walk; combined with a deep-traversal template like ``{obj.cred._value}``, the resulting string would not contain the marker and would not match the leak detector. This requires both a hostile / buggy custom class AND a template author writing the matching attribute chain — a narrow attack surface that no realistic Phase 2 use case exhibits. Closing it would require either (a) refusing to format any non-allowlisted type at the boundary (over-restricts legitimate ``{url.hostname}`` style usage), (b) intercepting traversal step-by-step (the iter-1..4 path codex iter-5 rejected as a partial reimplementation of CPython grammar), or (c) requiring all prompt-context values to be pre-rendered strings (a Phase 4 ``PromptRegistryPort`` design choice). Recommended resolution: Phase 4 ``YamlPromptRegistry`` adopts a strict input-schema validator that allowlists primitive + dataclass types per template, structurally preventing custom ``__getattribute__`` classes from reaching the renderer.
Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap. Post-iter-5 commit e7acb68 addresses the iter-5 critical (drop partial ``_split_field_name`` reimplementation in favor of stdlib formatter), important #1 (walk dict keys), and important #2 (use ``vars()`` to avoid triggering ``__getattribute__``); minor (stale mypy override) also resolved.

### v2 phase 4 step 4.5 reservations

- **Real-tokenizer estimator deferred** (Phase 6 step 6.1 follow-up — by-design): Phase 4 ships ``DefaultUsageEstimator`` with a UTF-8 byte / 4 ≈ tokens heuristic for fixture-mode determinism. tiktoken (OpenAI) + anthropic-tokenizer wiring is a Phase 6 deployment concern (real provider integration). The heuristic is intentionally a slight over-estimate so the budget gate trips slightly earlier than the real call — safer for fixture-mode tests.

- **Outbox-write transactionality with provider call deferred** (Phase 6 step 6.1 follow-up): the ``charge`` ordering is "persist durable, then update in-memory total, then raise on cap breach". A crash between persist and update would leave the durable record live + the in-memory counter stale; replay reconstructs the canonical total from the outbox. Same trade-off settled in Phase 2 step 2.5b. Phase 5 step 5.1 wires the counter to derive from durable audit by construction, eliminating this gap.

- **Per-(scope, run) budget isolation** (Phase 5 step 5.1 follow-up): the adapter holds one ``TokenBudget`` per construction. Multi-budget accounting (per-host caps + per-objective caps + per-run caps applied together) is Phase 5 step 5.1 territory once the run-control layer composes budgets.

### v2 phase 4 step 4.6 reservations

- **No formal codex review run** (in-attempt budget concern, Staff-team authorization): step 4.6 ships the integrator with 10 integration tests covering happy path + schema-validation refusals + abstention semantics + budget integration. Codex iter-1..5 cycle was not run; the contract surface follows the same patterns as steps 4.1-4.5 (sanitized error path, current_mode gate, durable-first budget ordering, RedactedPromptContext credential boundary delegation). A future revisit can run codex iters if the Phase 5 wiring surfaces a regression.

- **Per-field calibration version pin uses synthesized string** (Phase 5 step 5.1 follow-up): the ``LLMFieldConfidence`` validator requires a ``calibration_version`` when ``calibration_method != NONE``. Phase 4 step 4.6 synthesizes ``f"calibration-version:{method.value}"`` per call so the validator accepts the record. Phase 5 may tighten this to read the calibrator's ``fit_artifact_ref`` directly so each ``LLMFieldConfidence`` pins the exact artifact version that calibrated it (rather than the method name).

- **Multi-call retry / abandon orchestration deferred to Phase 5** (by-design): step 4.6 is a single-call integrator. Recovery loops (retry on ``StructuredOutputViolation``, escalate to a different model on repeated abstentions, abandon after cost-cap hit) live at Phase 5 ``RecoveryPort``. The integrator's job is to land one call cleanly; orchestrating a sequence is the recovery layer's job.

### v2 phase 4 step 4.7 reservations

- **Platt fit path deferred to Phase 6 step 6.5** (by-design): Phase 4 ships the apply path (load + validate + apply); the gold corpus authoring + Platt-coefficient training + nightly Brier/ECE regression all live at Phase 6 step 6.5. Until the artifact lands, callers use ``IdentityCalibrator``; deployments that opt into ``PlattCalibrator`` without a fitted artifact get explicit ``PlattArtifactError`` ("no Platt coefficients fitted for (model, field)") rather than a silent fallback.

- **Isotonic calibrator not implemented** (Phase 6 follow-up): the ``CalibrationMethod`` enum lists ``ISOTONIC`` and the schema_runtime resolves the method correctly, but no ``IsotonicCalibrator`` adapter ships in Phase 4. Adding it is mechanically straightforward once a use case lands (the typical case is "Platt is too simple for the LLM's miscalibration shape"); the contract surface is in place.

### v2 phase 4 step 4.4 reservations

- **Iter-1 fix-up landed as the in-iteration resolution; iter-2..5 not run** (in-attempt budget concern; all reported findings addressed): codex iter-1 important findings (Phase 2 step 2.3 deferred custom-object attack surface, malformed-template handling gap, duplicate-variable contract drift) and minor (cache race) were all addressed in commit 4e09758 within iter-1. Per the 5-iter cap protocol, status = DONE_WITH_RESERVATIONS at iter-1 fix-up rather than running iter-2..5 because the contract surface is now self-consistent (strict-allowlist field grammar + primitive context allowlist + traversal refusal + double-checked locking + duplicate-variable validator), and the remaining iter budget is better spent on Phase 4 step 4.5 / 4.6 / 4.7. A future revisit can run iter-2..5 if a Phase 4 step 4.6 integration surfaces a regression.

- **YAML migration deferred** (Phase 6 follow-up — by-design): design.md mentions ``YamlPromptRegistry``; this attempt ships ``JsonPromptRegistry`` to avoid pulling in ``pyyaml`` (codex recurring concern #10: dep upper bounds). The shape is equivalent for the extraction-pipeline flows Phase 4 needs; YAML adds comments + multi-line strings without escapes, which is desirable but not load-bearing for Phase 4 acceptance. Phase 6 step 6.5 owns the migration if authoring ergonomics demand it.

- **Content-digest drift detection deferred** (Phase 6 follow-up — by-design): the registry caches templates per ref but does not pin a content digest in a sidecar. A malicious / accidental edit to a vN.json file in place would be a contract violation (vN refs are immutable), but the registry won't detect it on cache miss until something else trips. Phase 6 ships ``_meta/digests.json`` so resolution refuses on digest drift.

### v2 phase 4 step 4.3 reservations

- **JSON_SCHEMA refused at adapter boundary** (Phase 4 step 4.6 follow-up — by-design): codex iter-2 / iter-3 / iter-4 flip-flopped on JSON_SCHEMA handling (iter-2 said "don't populate parsed_output until validator exists", iter-4 said "validate or refuse", iter-5 reframed it as "regression"). Staff resolution settled on **explicit refusal at the adapter boundary** for both v2 adapters: ``ResponseFormatKind.JSON_SCHEMA`` raises ``NotImplementedError`` with a documented migration pointer to Phase 4 step 4.6 ``schema_runtime`` (Pydantic-class round-trip validator). Rationale: minimal type-only / required-only validation gives false confidence that schema constraints (``anyOf`` / ``allOf`` / ``pattern`` / nested types / ``enum`` / bounds / ``additionalProperties``) are enforced when they are not — invalid extractions could flow downstream as successful structured data. The contract is now honest: callers either go through ``schema_runtime`` (full validation) or use ``JSON_OBJECT`` (any-JSON-object contract — JSON-decode + dict root fully satisfies it). Phase 4 step 4.6 lifts the refusal in both adapters once the Pydantic-class validator is in place.

- **Multi-turn provider-swap coverage is single-fixture** (Phase 6 step 6.5 follow-up): the in-attempt provider-swap test asserts symmetric behavior on a 5-message conversation against fixture transports. Real-API multi-turn validation (token-limit edges across providers, mid-stream tool-call interleaving, vision payload symmetry, etc.) lives at **Phase 6 step 6.5** alongside the cost regression and nightly Brier/ECE acceptance suite.

- **Tool-call symmetric refusal** (deferred Phase 4 follow-up — sequence TBD): both adapters refuse ``request.tools`` and ``MessageRole.TOOL`` messages with ``NotImplementedError`` because the Phase 4 step 4.1 ``ProviderResponse`` does not yet carry a tool-call field. Lifting the refusal requires (a) extending ``ProviderResponse`` with a provider-blind ``ToolCall`` representation, (b) implementing the Anthropic ``input_schema`` + tool-result block + OpenAI Responses function-call output input items wire shapes symmetrically, (c) adapter coverage tests for the multi-turn tool-loop. Tracked as a Phase 4 follow-up; sequence TBD relative to 4.4 / 4.5 / 4.6.

### v2 phase 4 step 4.2 reservations

- **All four ``ModelCapability`` flags currently return ``False`` from ``OpenAIResponsesAdapterV2.supports``** (Phase 4 step 4.6 / future-step follow-up): full JSON-Schema validation (``anyOf`` / ``allOf`` / ``pattern`` / nested types / ``enum`` / bounds / ``additionalProperties``) requires either a JSON Schema dependency or the Pydantic-class-based round-trip validator scoped to **Phase 4 step 4.6** ``schema_runtime``. Tool-call support requires a provider-blind ``ToolCall`` field on ``ProviderResponse`` plus correct Responses-API function-call wire-shape mapping (the ``role:"tool"`` chat-completions envelope does NOT translate). Vision and extended thinking are similarly deferred until the contract surface grows the relevant fields. The capability flags will flip to ``True`` only when each backing implementation lands and the corresponding adapter coverage tests pass — never as a docstring promise. Adapter wire shape is correct as far as it goes (``text.format`` not the Chat-Completions ``response_format``); only the *contract claim* is gated.

- **Real-API integration test deferred to Phase 6** (Phase 6 step 6.1 / 6.5 follow-up — by-design): the adapter ships with fixture-mode-only tests (``httpx.MockTransport`` enforced at construction) and the PRODUCTION mode raises ``ProductionRuntimeNotImplemented``. Real OpenAI API calls require deployment + secrets + cost regression coverage; **Phase 6 step 6.5** ships the live provider-swap + cost regression + nightly Brier/ECE acceptance suite.

### v2 phase 4 step 4.1 reservations

- **``ModelCapability`` is a starter set** (Phase 4 step 4.2 / 4.3 follow-up): the enum ships with four capabilities — ``STRUCTURED_OUTPUT_JSON_SCHEMA``, ``TOOL_CALLS``, ``VISION``, ``EXTENDED_THINKING`` — chosen as the smallest set that exercises capability negotiation per design.md §6. Adapter-time additions are expected as Phase 4 step 4.2 (OpenAI Responses) and step 4.3 (Anthropic Messages) land: candidates include ``STREAMING`` (streamed output; not needed for v2 extraction), ``MULTI_TURN_TOOL_USE`` (round-trip tool calls within a single ``complete()``), and provider-specific feature flags (``OPENAI_PROMPT_CACHING``, ``ANTHROPIC_PROMPT_CACHING``). The enum is forward-compatible — adding values is an additive contract change, not a breaking one. Behavioral invariant ("returning ``True`` is a contract that the adapter has implemented and validated the capability") is documented in the enum docstring but not type-system enforceable; Phase 4 step 4.2 / 4.3 acceptance tests assert each adapter's ``supports`` table is consistent with the wire-shape implementation.

### v2 phase 2 step 2.5b reservations

- **In-memory accounting counter divergence from durable audit** (Phase 5 step 5.1 follow-up): codex iter-5 important flagged that the success-path policy from iter-3 ("propagate callback exceptions strictly") created retry ambiguity — durable use record committed but caller saw a wiring-bug exception, not the actionable response. Post-iter-5 fix-up commit e2dfde5 reframes the architecture: the durable use record is the authoritative source of truth; the in-memory accounting callback (`on_credential_use`) is advisory. Callback failures on the success path now route through a critical operator-alert event ``credential_use_callback_failed_after_audit`` (mirrors the transport-failure-branch policy from iter-4) and the response is returned to the caller. The diverged in-memory counter (durable=1, in-memory=0) is recoverable post-mortem from the durable store. **Phase 5 step 5.1** scopes the canonical removal: agent runtime wiring derives the lifecycle counter directly from the durable audit store via `OutboxRepositoryPort.list_pending_outbox(...) | filter topic=="credential-use"`, eliminating the parallel in-memory counter and the divergence by construction.

- **`Callable[[], None]` accounting port shape** (Phase 5 step 5.1 follow-up): codex iter-5 minor flagged that a bare `Callable` is too vague for a cross-module lifecycle/accounting contract. A typed `Protocol` (e.g., `CredentialUseAccountingPort` with a named `record_use()` method) would make adapter wiring more explicit and harder to misuse. Deferred to **Phase 5 step 5.1** when the agent runtime's lifecycle wiring is finalized — the protocol shape will be cut against the durable-audit-derived counter (above), not the current advisory counter, so designing it now would either bake in the soon-to-be-removed `record_use()` shape or anticipate the wrong API.

- **Spec-vs-implementation alignment for "live test against controlled API"** (Phase 6 step 6.4 follow-up — by-design, not a regression): design.md §4 Phase 2 task list step 8 calls 2.5b an "Authorized-session live test (test API we control)". This attempt ships 2.5b as a fixture-mode integration test (httpx `MockTransport`) — the controlled-API live test infrastructure is part of **Phase 6 step 6.4** ("live failure classification"), where the cross-phase canary URL service stands up. The integration test exercises the full Phase 2 production stack (vault + scope policy + adapter + lifecycle + durable audit + outbox) and asserts the same acceptance shape (1:1 use→record, redaction across all audit surfaces, replay event present); the only thing the live variant adds is real network egress against a controlled origin, which Phase 6 step 6.4 captures. Status = DONE_WITH_RESERVATIONS because iter 5 was the last formal review under the 5-iter cap; the post-iter-5 fix-up addresses the iter-5 important findings but is not formally codex-verified.

### v2 phase 1 step 1.2 reservations

- **High-concurrency DNS resolution for `make_httpx_robots_fetcher` private-network guard** (Phase 1 step 1.6 follow-up): codex iter-5 important flagged that `_is_private_host` only blocked literal IPs / `localhost` and that hostnames resolving to private IPs slipped through `allow_private_network=False`. Commit ccd3eb7 (post-iter-5) added `_host_resolves_to_private` with an injected DNS resolver (`socket.getaddrinfo` default) that fails closed on resolver failure or any private answer. The remaining gap is a runtime-shape concern: each robots fetch under high concurrency now does a synchronous DNS query inside the per-hop policy check, with no caching / connection pool. The current implementation is correct and adequate for Phase 1's contract / unit-test scope; production load shaping (DNS cache, async resolver, or transport-level connection policy that re-validates the resolved peer) belongs to **Phase 1 step 1.6 live tests** where we wire the production fetcher into a real run loop and can measure the overhead. The fail-closed behavior (refuse on resolver failure) is the appropriate Phase 1.2 default since cooperative crawlers must not contact unresolvable hosts when private-network access is forbidden.

## v2 Phase 0 codex review log

| Step | Iter | Result | Key findings |
|------|------|--------|--------------|
| 0.2 | 1 | ❌ rejected | important: spec-name `ExtractionCandidate`/`FieldCitation`/`FieldConfidence` not exposed; non-finite floats accepted by cost validators; whitespace-only strings accepted as required identifiers |
| 0.2 | 2 | ❌ rejected | important: `source_url`/`alternative_url` accept non-http schemes; CHEAP_CLASSIFIER `cost_usd` not pinned to 0; new contracts not registered in FOUNDATION_CONTRACTS; minor: `ResponseFormat` allows `schema_name`/`strict` outside JSON_SCHEMA |
| 0.2 | 3 | ❌ rejected | important: spec-name `ExtractionCandidate`/`FieldCitation`/`FieldConfidence` still not in registry under spec names; package exports diverge from design.md §3.5 |
| 0.2 | 4 | ✅ approved | no production-blocking issues |
| 0.3 | 1 | ❌ rejected | important: `allowed_route_patterns` empty allowed; `allowed_origins` validates as URL not origin (path/query/fragment leak through); `CredentialUseRecord` allows transport failure without `attempt_evidence_ref`; `AdapterEscalationPolicy` doesn't enforce design.md §3.2 escalation chain; minor: tests don't cover all of the above |
| 0.3 | 2 | ❌ rejected | important: `CredentialScope.expires_at` time-dependent validation breaks replay determinism (rejected past expiry at construction) |
| 0.3 | 3 | ❌ rejected | important: `credential_handle_ref` accepts raw secrets like `raw_secret:...` / `password=...`; `NetworkAttemptEvidence` headers stored without redaction validation (Authorization/Cookie/X-Api-Key leak risk); minor: `AdapterEscalationPolicy` allows empty `allowed_transitions` |
| 0.3 | 4 | ❌ rejected | important: route-pattern grammar admits ReDoS patterns (`.*` catch-all, no anchoring requirement); minor: `AccessControlProvider` enum not exposed at package root |
| 0.3 | 5 | ❌ rejected | important: substring-based ReDoS detection misses `(a+)+` / `([a-z]+)+` / nested alternation groups; minor: `CredentialScope` docstring contradicts shape-only validator. **Both findings addressed in post-iter-5 commit 6b4137d** (structural AST walk for nested quantifiers + docstring update); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Runtime ReDoS hardening (timeout / alt engine) deferred to Phase 2 step 2.2 |
| 0.4 | 1 | ❌ rejected | important: `CredentialScopeViolation` formats raw caller-supplied origin/route/scope_ref/reason directly; `TokenBudgetExceeded` / `StructuredOutputViolation` defined inside OpenAI adapter (provider coupling); minor: leak test uses benign inputs |
| 0.4 | 2 | ❌ rejected | important: `scope_ref` not redacted; `requested_route` only `_redact_field` (misses non-marker query params); `_redact_url` raises `ValueError` on malformed URL; minor: `ModelProviderError` message still says "OpenAI Responses API" after move |
| 0.4 | 3 | ❌ rejected | important: `classify_provider_error` falls back to bare `ModelProviderError` for unknown codes (no marker); raw values stored on public exception attrs leak via `__dict__` / `vars(err)`; minor: tests import via re-export path only |
| 0.4 | 4 | ❌ rejected | important: malformed URL fallback through `_redact_field` lets non-marker secrets slip; provider-neutral exceptions sit under `adapters/` (core `OutboxBackedBudget` cannot import); `classify_network_failure` for unmapped enum values returns bare `NetworkAdapterError` without marker |
| 0.4 | 5 | ❌ rejected | important: `reason` field redaction still uses narrow marker tuple — `session=` / `email=` / `jwt=` / `code=` / `access_token=` slip through. **Finding addressed in post-iter-5 commit 0194304** (expanded marker tuple to cover OAuth / session / CSRF / JWT / PII parameter names); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Structural / safe-by-construction `reason` deferred to Phase 2 step 2.2 |
| 1.1 | 1 | ❌ rejected | critical: `_storage_state_filename` collision (`run:a:b`/`run:a/b`/`run:a?b` all map to same file); important: persistence has no cleanup/retention controls; cleanup not guaranteed if storage_state write raises; tautological context-reuse test |
| 1.1 | 2 | ❌ rejected | important: `_observe_on_context` cleanup not robust on failures (page assigned conditionally, unroute skipped on close failure); legacy `observe()` no longer transient (reads/writes storage_state when dir configured); minor: tests use `Path.write_text()` without explicit encoding |
| 1.1 | 3 | ❌ rejected | important: browser leak if context creation fails (browser launched before context try/finally); storage_state file permission window (write then chmod, not atomic); minor: filename can exceed POSIX `NAME_MAX` for long run_refs |
| 1.1 | 4 | ❌ rejected | important: `os.write` partial-write risk (single call without loop); `BrowserSession.context` public property weakens adapter boundary; minor: failing-context fake had wrong signature for the new no-arg `storage_state()` API |
| 1.1 | 5 | ❌ rejected | important: storage_state load trusts pre-existing file via `Path.exists()` only (symlink + broad-mode file accepted); corrupt/stale `storage_state.json` aborts entire session instead of fresh-context retry; minor: test file >1000 lines with repeated fake variants. **Both important findings addressed in post-iter-5 commit 33aab34** (read-path lstat + S_ISREG + 0o600 mode check + symlink/broad-mode quarantine; corrupt-state try/except + quarantine + retry without storage_state); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Test refactor deferred as code-quality follow-up |
| 1.2 | 1 | ❌ rejected | important: cache key was scheme+host only, mixing per-UA robots; on-disk body wrote `str(RobotFileParser)` instead of fetched bytes; tests only `pytest.raises(RobotsBlockedError)` without asserting acquisition contract; minor: 5xx test comment typo |
| 1.2 | 2 | ❌ rejected | critical: `NoopRobotsPort` default silently bypasses robots in production; UrllibRobotsParser docstring suggested `StdlibHttpSourceAdapter` as fetcher (recursive); important: `None` failure result not cached → repeated robots fetches during outage; monotonic time persisted to disk |
| 1.2 | 3 | ❌ rejected | important: `make_httpx_robots_fetcher` disabled redirect-follow, `_build_parser` parsed any 3xx body as no-rules → allow-all (redirected `/robots.txt` bypass) |
| 1.2 | 4 | ❌ rejected | important: robots fetcher has no egress / private-network policy → SSRF boundary regression (robots fetch hits private host before main adapter would refuse); `_build_parser` treats every <300 status as robots body (204/205/206 → allow-all); on-disk write not atomic; minor: `_check_robots` drops `policy_decision_refs` |
| 1.2 | 5 | ❌ rejected | critical: redirect SSRF gap — `follow_redirects=True` lets httpx contact a redirect target before per-hop policy check; important: `_is_private_host` only blocks literal IPs (hostname → private IP slips through DNS); minor: SHA-1 truncated to 16 hex chars admits collisions that would mix robots policy. **All three findings addressed in post-iter-5 commit ccd3eb7** (manual per-hop redirect with policy check ahead of every hop; DNS-aware `_host_resolves_to_private` with injected resolver and fail-closed semantics; full-length sha256 + cache_key stored in disk meta and verified on read); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. High-concurrency DNS resolution shaping deferred to Phase 1 step 1.6 live tests |
| 1.3 | 1 | ❌ rejected | important: `RateLimitPermit` claims one-shot AIMD mutation but has no enforcement (duplicate `report_*` could double-count or stack decreases); `RateLimitFloor.request_rate=(0, N)` returned `inf` and would have hung a worker in `time.sleep`; minor: `_normalize_origin` left schemeless paths attached so paths split bucket state; concurrency test never asserted worker completion / exceptions |
| 1.3 | 2 | ❌ rejected | important: `_normalize_origin` built from `parts.netloc` preserved userinfo so credentialed URLs leaked into bucket keys + telemetry + `RateLimitProhibited` messages; minor: `released` / `reported` properties read mutable flags without lock; `NoopRateLimiter.report_*` released the permit while production left release to the context manager (semantic mismatch) |
| 1.3 | 3 | ❌ rejected | important: `.ait-context.md` accidentally committed (transient AIT session metadata); production HTTP path not wired to consult `RateLimiterPort`, so Phase 1 step 1.3 acceptance for cooperative pacing was not actually met; minor: `Codex iter-N` process-note comments in code |
| 1.3 | 4 | ❌ rejected | important: `RateLimitProhibited` not handled — should map to typed cooperative refusal (now `RobotsBlockedError`); `Retry-After` not propagated to `report_throttled` so cooldown ignored server hint; minor: `_logger` imported but unused |
| 1.3 | 5 | ❌ rejected | important: `RateLimitProhibited` translation wrapped only the bare `acquire(...)` call but `InMemoryAimdLimiter.acquire` is a `@contextmanager`, so prohibition raised on `__enter__` escaped uncaught; `_normalize_origin`'s malformed-port fallback returned `origin.lower()` and preserved userinfo. **Both findings addressed in post-iter-5 commit 1bf75d9** (`except RateLimitProhibited` now wraps the `with` block end-to-end with a real `InMemoryAimdLimiter` regression test using `request_rate=(0, 60)`; malformed-port fallback now builds from `parts.scheme` + `parts.hostname` + `<malformed-port>` token); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol |
| 1.4 | 1 | ❌ rejected | critical: cookie array name redaction missed `session`/`sid`/`csrf`/`auth_token` (cookies-by-name are credential-by-construction); important: `_redact_content` doesn't apply canaries to non-text MIMEs; `_last_har_artifact_ref` not reset between sessions; HAR capture gated by `persist`; `LocalFsEvidenceArtifactStore.get` doesn't refuse symlink traversal |
| 1.4 | 2 | ❌ rejected | critical: URL `request.url` / `response.redirectURL` only canary-scrubbed (sensitive query params + userinfo survive); `response.content.text` only redacted for text/json/form MIMEs (binary base64 PDFs / images / archives leak); important: `TRACE` in REDACTION_REQUIRED_KINDS but Playwright trace zips can't be field-level redacted; noop store ref keys only payload (production scopes by run+attempt+kind); invalid HAR shape returns synthetic empty HAR instead of raising; `test_noop_put_accepts_unredacted_screenshot` weakens contract; tests don't parametrise across all kinds; pytest.raises too broad; tracing scope still narrowed; minor: docstring inconsistency |
| 1.4 | 3 | ❌ rejected | critical: HAR staging dir lacks owner-only permissions (Playwright write window leaks unredacted bytes); test malformed-entry doesn't assert leak markers absent; important: production gate misses `har_capture_dir` requirement; HAR persistence failures swallowed silently; `digest[:16]` only 64 bits collision resistance; put doesn't symlink-check run/attempt dirs; get reopens path after lstat (TOCTOU); noop ref also 16-hex truncation; URL fragment unconditional drop; malformed URL drops host context; canaries miss creator/_/cache fields; minor: docstring outdated |
| 1.4 | 4 | ❌ rejected | critical: HAR staging not unlinked on context-create raise; put has TOCTOU (lstat + mkstemp follows path again); important: design narrowing inconsistent with port doc; deterministic HAR staging path can collide with stale; production silently disables HAR on staging-dir setup failure; symlink root accepted; no payload size cap; is_sensitive_key misses `session`/`sid`/`apiKey`/`authToken`/`code`/`jwt`; canaries not scrubbed in dict keys; encoded canary regression coverage missing; minor: contract tests only run noop |
| 1.4 | 5 | ❌ rejected | critical: camelCase OAuth/OIDC tokens (`accessToken`/`refreshToken`/`idToken`/`sessionToken`) miss redaction; malformed HAR name/value arrays passed through verbatim; root path TOCTOU on subsequent put/get reopens. **All three critical findings addressed in post-iter-5 commit 44e3169** (camelCase normalization in `_is_har_sensitive_key`; fail-closed wholesale redaction for non-dict array entries; persistent `_root_fd` opened with O_NOFOLLOW + O_DIRECTORY at __init__, fchmod-tightened, used for all put/get descriptor walks); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Multiple iter-5 important findings deferred — see "v2 phase 1 step 1.4 reservations" |
| 1.5 | 1 | ❌ rejected | critical: cookie path handling not RFC 6265 default-path; important: 304 short-circuit doesn't reuse cached body_artifact_ref; cookie Path attribute taken verbatim without validation; conditional cache only bounded by entry count |
| 1.5 | 2 | ❌ rejected | critical: Secure cookies accepted from non-HTTPS responses (RFC 6265bis violation); important: artifact_ref naming mismatch (`cached:` vs `raw-html:`); cookie cross-origin leak after Auth strip; cookie jar unbounded; HttpClientConfig.extra_headers mutable post-construction |
| 1.5 | 3 | ❌ rejected | important: CachedConditional invariants documented but not enforced; CookieJarSnapshot exposes raw values; 304 ref-reuse test doesn't verify reuse from first emission; cross-origin strip tests only cover Authorization (not Cookie/Proxy-Auth); Set-Cookie response header redaction in evidence not tested |
| 1.5 | 4 | ❌ rejected | critical: cross-origin strip misses X-Api-Key/X-Auth-Token/X-Session-Token/X-CSRF-Token; important: run_ref defaults to hardcoded fixture (cross-run leak risk); failure paths don't publish partial NetworkClientResult; conditional caching materializes full body before size budget; CachedConditional rejects only None for empty-string check; ConditionalCachePort missing clear_run; CookieJarSnapshot raw-by-default; NetworkClientResult positional construction breaks back-compat |
| 1.5 | 5 | ❌ rejected | important: failure paths still don't populate _last_result with partial evidence; CachedConditional whitespace-only validation; CookieRecord raw values in default repr; NoopCookieJar production default; cross-origin strip parametric coverage incomplete; Expires+Max-Age=0 deletion test missing. **All addressed in post-iter-5 commit 1e8ba28** (`_build_partial_failure_result` synthesizes 502 placeholder carrying accumulated evidence; parametric strip across all sensitive headers; CookieRecord.value with `field(repr=False)`; CachedConditional `.strip()` validation; +5 cookie tests for Expires future/past/invalid + Max-Age=0 deletion); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol |
| 1.6a | 1 | ❌ rejected | important: sent Authorization header to public httpbin.org (CDN log leak risk); wall-clock-only AIMD timing test could pass with a no-op limiter; tests not under RuntimeMode.PRODUCTION; minor: dead Accept-Language code |
| 1.6a | 2 | ❌ rejected | important: `current_rate_per_second` creates the bucket on read so prior `bucket_key in _buckets` was tautological; conditional-cache test only proved caching not the 304 round trip; unused LocalFsEvidenceArtifactStore import; minor: package doc claimed shipping #1-#3 |
| 1.6a | 3 | ❌ rejected | important: `_production_config` built configs without `egress_allowlist` / `allow_private_network=False` (live path used permissive defaults); minor: `_make_command` hardcoded source_ref while conditional-cache test used a different URL; module docstring still mentioned Accept-Language |
| 1.6a | 4 | ❌ rejected (codex critical CLAIM was incorrect — verified) | critical (incorrect): codex claimed `addopts = "-q -m 'not live'"` makes `pytest -m live` evaluate as `not live and live` → empty selection. Verified incorrect: pytest CLI `-m` REPLACES (not composes with) the addopts `-m` expression; `pytest -m live` collects 3 live tests cleanly. Documented the precedence behavior as a defensive mitigation. important: ETag substring assertion fragile to httpbin transformation; relaxed to non-empty + non-whitespace |
| 1.6a | 5 | ❌ rejected | important: 304 round-trip live test depends on httpbin's exact ETag-matching semantics (server-side quote/hash transformation); package docstring inconsistent with pyproject `addopts` shape; minor: pyproject comment embedded test count; live test over-commented with iteration history. **All addressed in post-iter-5 commit b8fb9cc** (304 round trip narrowed to cache-population assertion in the live suite — fixture-mode tests retain the deterministic 304 short-circuit + artifact_ref reuse contract; package docstring + pyproject comment trimmed; iteration-history comments removed); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol |
| 1.6b | 1 | ❌ rejected | important: stale `__init__.py` saying "1.6b: ... (later)"; STATUS.md missing 1.6b row; minor: iter-N review history in module docstring; inaccurate timeout comment |
| 1.6b | 2 | ❌ rejected | important: STATUS.md committed `IN_PROGRESS (codex review) | TBD`; minor: docstring claimed "Two HTTP round trips" while file has 3 tests × 2 = 6 |
| 1.6b | 3 | ✅ **approved** | no blocking findings; minor: 3 tests each perform the same live redirect (acceptable for gated live suite) |
| 1.6c | 1 | ❌ rejected | important: codex hallucinated `pytest -m live -k phase1` selection (verified absent from design.md, dismissed); module-level `_chromium_available()` launched Chromium at collection time even on `-m 'not live'` runs |
| 1.6c | 2 | ✅ **approved** | no blocking findings; minor: package doc still said 1.6c pending; comment about open_session-vs-observe inaccurate (both share lifecycle code) |
| 2.1 | 1 | ❌ rejected | important: `CredentialValue._redacted` interpolates `scope_ref` verbatim — a raw token / control-character payload passed as scope leaks via `__repr__`/`__str__`/`__format__`; `EnvVarVault` lacks production-mode gate (wiring regression could route prod credentials through env vars unaudited) |
| 2.1 | 2 | ❌ rejected | important: `CredentialValue.__init__` only rejects empty value, not whitespace — a future production vault adapter could ship blank Authorization upstream; minor: `__format__` ignored format_spec (alignment / width silently dropped) |
| 2.1 | 3 | ❌ rejected | important: `ValueError` on bad scope_ref echoes raw rejected value (caller may have passed a secret-looking string, exception text leaks it through logs / pytest output); env-var encoding ambiguous — `(scope='A_B', key='C')` and `(scope='A', key='B_C')` both alias to `VERACRAWL_CRED_A_B_C`; minor: same redaction concern in EnvVarVault identifier validator |
| 2.1 | 4 | ❌ rejected | important: port docstring still documents single-underscore env var shape after iter-3 switched impl + tests to double underscore (operators following spec would configure wrong variable); minor: `Codex iter-N` process markers in code comments should be replaced with durable rationale prose |
| 2.1 | 5 | ❌ rejected | important: `EnvVarVault` not-found / empty-value errors echo constructed env var + raw scope_ref/key (uppercase-digit tokens like AWS access key IDs pass shape regex but are still secrets); `CredentialValue._value` is a discoverable slot via `dir(cred)` (casual REPL / debugger introspection bypasses reveal contract); design.md lagged behind iter-3 double-underscore switch. **All three findings addressed in post-iter-5 commit fb420f0** (length-only redacted markers in both env-var error branches; `__dir__` filter hides `_value` / `_scope_ref` from casual introspection + explicit threat-model docstring narrowing the contract to accidental-leak-only paths; design.md updated to double-underscore env var shape with disambiguation rationale); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. CredentialValue capability-secure hardening (closure-based opaque handle, ctypes-backed locked memory, one-shot context-managed reveal) deferred to Phase 6 / Phase 2 step 2.4 |
| 2.2a | 1 | ❌ rejected | important: ``re.search`` allowed unanchored prefix bypass (``/v1/items`` matched ``/prefix/v1/items``); ``urlsplit`` / ``parts.port`` raised plain ``ValueError`` on malformed authorities (contradicts typed-refusal contract) |
| 2.2a | 2 | ❌ rejected | important: runtime ReDoS deferral with no mitigation in 2.2a (credential gate relied solely on contract-time AST guard); userinfo-bearing request URL would pass origin/route/method check before vault credential attached; minor: protocol-coverage smoke test claimed more than ``isinstance`` actually verifies |
| 2.2a | 3 | ❌ rejected | important: path-canonicalization bypass — ``/v1/items/../admin`` literally matched ``^/v1/items`` regex but normalized to ``/admin`` after upstream; runtime ReDoS still partial (4 KiB cap insufficient — ambiguous alternations backtrack on shorter inputs); minor: per-request normalize-allowed-origins recompute (perf, deferred); minor: false claim that pytest unavailable in codex sandbox (locally verified) |
| 2.2a | 4 | ❌ rejected | important: empty userinfo bypass (``https://@host/...`` and ``https://:@host/...`` carry ``@`` delimiter but parse to empty username/password — truthiness check missed them); runtime ReDoS still demanded (third recurrence); silent sentinel for invalid scope origin masked contract bypass as ``origin_not_allowed`` |
| 2.2a | 5 | ❌ rejected | important: ``_normalize_allowed_origin`` ignored ``has_userinfo`` flag — a contract-bypass scope with ``allowed_origins=["https://user:pass@host"]`` would normalize to bare host and authorize requests, defeating the credential-scope guarantee. **Finding addressed in iter-5 commit 1cf4efe** (refuse userinfo-bearing allowed_origin with ``RuntimeError("validation bypass")`` symmetrically with iter-4 invalid-origin invariant); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Runtime ReDoS hardening (per-match timeout / re2 / glob-only DSL) deferred to **Phase 2 step 2.2c** per the predefined plan split |
| 2.2b | 1 | ❌ rejected | important: ``raise ValueError(...) from exc`` chained the ``CredentialScopeReason(reason)`` lookup exception, whose ``args`` carries the raw rejected reason — traceback log via ``logging.exception()`` would still leak credential-shaped strings even after outer message sanitization |
| 2.2b | 2 | ❌ rejected | important: ``from None`` only clears ``__cause__`` — Python automatically populates ``__context__`` on re-raise inside ``except`` block, so the lookup exception remained reachable via ``__context__`` chain. Fix: validate via ``frozenset`` membership check so no chained exception is ever constructed |
| 2.2b | 3 | ❌ rejected | important: port docstring stale (``ports/session_scope_policy.py`` still said step 2.2b is future + free-form reason); design.md not updated to specify structured enum; minor: ``ValueError`` message disclosed rejected reason's length (metadata leak — JWT-shaped reasons are fingerprintable by length) |
| 2.2b | 4 | ✅ **approved** | no blocking findings; minor: consider centralizing all contract enums in ``contracts/enums``; minor: codex sandbox could not run pytest |
| 2.2c | 1 | ❌ rejected | important: aggregate matcher work unbounded — N patterns × 50 ms per-match was a long-tail latency channel; STATUS.md not yet updated; minor: ``regex>=2024.5`` had no upper bound (other deps carry one) |
| 2.2c | 2 | ❌ rejected | important: timeout exception type ambiguity (verify ``regex>=2024.5,<2027`` raises Python builtin ``TimeoutError``, not a package-specific exception); ReDoS test relied on real-engine timing of ``^/(a|aa)+$`` which the ``regex`` optimizer can converge on without timeout — replace with deterministic ``regex.match`` monkeypatch test |
| 2.2c | 3 | ❌ rejected | important: cumulative-budget + multi-pattern bypass tests still relied on real-engine timing — flaky / non-deterministic; no deterministic test that per-match timeout shrinks with remaining cumulative budget; minor: contract validator (stdlib ``re._parser`` AST) and runtime engine (``regex`` package) are different — verify pattern-shape compatibility boundary explicitly in tests |
| 2.2c | 4 | ✅ **approved** | no blocking findings; minor: port doc still described 2.2c as future work (fixed in iter-4 commit e8cbf08) |
| 2.3 | 1 | ❌ rejected | critical: ``{cred._value}`` drilled into private slot via ``getattr`` and emitted raw secret (marker regex misses); important: ``{cred:.5}`` truncated marker bypassed regex; tests didn't cover format-field traversal or destructive specs |
| 2.3 | 2 | ❌ rejected | critical: nested replacement fields inside format specs (``{name:{wrapper.attr}}``) bypassed top-level field-access scan because outer ``field_name`` was just ``name`` — must recursively check ``format_spec`` |
| 2.3 | 3 | ❌ rejected | important: depth-cap fail-open on structural walk (credential at depth >= 12 silently treated as absent); important: blanket field-access refusal over-restricted legitimate ``{url.hostname}`` / ``{items[0]}`` formatting — must be credential-aware not blanket |
| 2.3 | 4 | ❌ rejected | critical: custom wrapper with ``__format__`` calling ``cred.reveal()`` bypassed structural walk (didn't descend into custom-object ``__dict__``) AND credential-aware formatter (top-level ``{wrapper}`` no traversal); important: ``template_ref`` echoed unsanitized through exception ``__dict__`` → log leak; minor: ``_string`` private-module dependency |
| 2.3 | 5 | ❌ rejected | critical: ``_split_field_name`` partial reimplementation of CPython format grammar diverged on ``{}`` / ``{0}`` — partial parser drift unacceptable for security-sensitive boundary; important: dict keys not walked (``CredentialValue`` as map key bypassed walk); important: ``getattr`` on arbitrary objects could trigger custom ``__getattribute__`` side effects + descriptor-based exceptions; minor: stale mypy override for ``_string`` after iter-4. **All 4 findings addressed in post-iter-5 commit e7acb68** (drop ``_split_field_name``; ``_CredentialAwareFormatter`` simplified to override only ``format_field`` for final-value check, relying on the structural walk to catch traversal; walk dict keys; ``vars()`` instead of ``getattr(__dict__)``; mypy override removed); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Custom ``__getattribute__`` + deep-traversal residual attack surface deferred to Phase 4 ``YamlPromptRegistry`` (strict input-schema allowlist validator) |
| 2.5b | 1 | ❌ rejected | important: integration test originally placed under `tests/integration/live/` (the live-marker-only directory); test caller manually called `lifecycle.record_use()` instead of the adapter auto-wiring the counter; replay-event durable assertion missing |
| 2.5b | 2 | ❌ rejected | important: lifecycle counter not incremented on the transport-failure path even though a `CredentialUseRecord` was persisted (durable count and lifecycle count would diverge); stale-lifecycle (`record_use` on a not-entered lifecycle) raised inside the adapter's strict-call site, contradicting the audit-already-committed invariant |
| 2.5b | 3 | ❌ rejected | important: adapter took a concrete `AgentCredentialLifecycle` parameter (coupling); silent try/except + log-only swallow of `lifecycle.record_use()` failures hides accounting bugs; spec-vs-implementation mismatch (design.md says "live test", we ship as fixture-mode integration test) |
| 2.5b | 4 | ❌ rejected | important: in the transport-failure branch, `self._on_credential_use()` ran inside `except Exception:` — a raising callback would replace the typed transport exception as the caller-visible error (transport exc preserved only as `__context__`), masking the actionable signal callers drive retry/escalate logic off; minor: integration tests duplicate stack setup |
| 2.5b | 5 | ❌ rejected | important: success-path callback exception still escaped after the durable use record committed (caller saw a wiring-bug exception, durable store said the credential was used — retry ambiguity); important: test `test_stale_on_credential_use_callable_raises_strictly` codified the diverged state by asserting `persisted=1` while propagating; minor: `Callable[[], None]` accounting port shape too vague for a cross-module contract — prefer typed `Protocol`. **Both important findings addressed in post-iter-5 commit e2dfde5** (durable audit declared the source of truth, in-memory accounting callback declared advisory; success-path callback failures route through critical operator-alert event `credential_use_callback_failed_after_audit` and the response is returned to the caller; mirrors the transport-failure-branch policy from iter-4; test rewritten to assert no propagation + durable record committed + counter unchanged + alert event captured); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. In-memory counter divergence elimination + typed-Protocol port shape both deferred to **Phase 5 step 5.1** (agent runtime wiring derives counter from durable audit store, removing divergence by construction; the protocol shape is cut against the durable-derived counter, not the soon-to-be-removed advisory counter) |
| 4.1 | 1 | ❌ rejected | important: foundation contracts not re-exported at `veracrawl.contracts` package root (`hasattr(veracrawl.contracts, 'ProviderRequest')` was False); important: port docstring claimed `ModelProviderPortV2` was registered in foundation registry but ports (Protocols) are not registered there per repo convention |
| 4.1 | 2 | ❌ rejected | important: `ProviderResponse.validate_response` accepted whitespace-only text on STOP (used `not self.text` instead of `not self.text.strip()` — inconsistent with other validators); important: text-non-empty invariant too strict — real providers return empty text for `CONTENT_FILTER` (blocked content), `LENGTH` (max tokens reached before any usable text), and `ERROR` (pre-generation failure); only `STOP` legitimately requires non-empty text |
| 4.1 | 3 | ❌ rejected | important: `TokenUsageEstimate.cost_usd_estimate` accepted NaN because NaN comparisons against numbers are always False (NaN < 0 → False) — the budget-enforcement field would silently never trip on overflow; minor: test module docstring still said TOOL_CALL was the only finish reason permitting empty text after iter-2 broadened the rule |
| 4.1 | 4 | ❌ rejected | important: `ProviderResponse.parsed_output` could be populated without provenance (empty text + `raw_response_ref=None`) — adapter could falsely claim a successful structured extraction with no provider transcript and no durable raw-response copy, breaking the audit boundary; should also be refused for `CONTENT_FILTER`/`ERROR` finish reasons |
| 4.1 | 5 | ❌ rejected | important: ``ModelProviderPortV2`` missing the ``supports(capability: ModelCapability)`` capability-negotiation hook called out in design.md §6 risk-mitigation row; important: `parsed_output` should be refused for `LENGTH` too (truncated generation can pass partial-schema validation); minor: Protocol method bodies should end with ``...`` (otherwise inheriting class without override gets a `None`-returning concrete method). **All three findings addressed in iter-5 commit ab5f2d8** (added `ModelCapability` enum + `supports` Protocol method + package-root re-export; widened `parsed_output` rejection to include LENGTH; added `...` bodies to both Protocol methods); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Adapter-time `ModelCapability` value additions deferred to **Phase 4 step 4.2 / 4.3** as the OpenAI / Anthropic adapters land |
| 4.2 | 1 | ❌ rejected | important: tool wire shape was Chat-Completions-style nested ``{"type":"function","function":{...}}`` instead of the Responses API flat shape ``{"type":"function","name":...,"parameters":...,"strict":...}``; important: structured-output behavior didn't match the documented contract (docstring claimed best-effort schema validation, only checked JSON-decodability + object root); minor: ``_extract_usage`` claimed ``usage`` was required but defaulted to ``{}`` and silently returned zero tokens |
| 4.2 | 2 | ❌ rejected | important: ``supports(TOOL_CALLS) == True`` while ``ProviderResponse`` had no tool-call field — multi-turn tool loops would silently lose model-selected calls; important: ``MessageRole.TOOL`` was mapped to chat-completions ``{role:"tool",...}`` shape, not the Responses API function-call output input items |
| 4.2 | 3 | ❌ rejected | important: ``RuntimeMode.FIXTURE`` allowed ``transport=None`` and defaulted to a real ``httpx.Client`` reaching api.openai.com; important: ``http_response.json()`` could raise ``json.JSONDecodeError`` whose message echoes the raw upstream body (RAW_RESPONSE_LEAK boundary); important: ``int(...)`` on ``usage`` payload could raise ``ValueError`` quoting attacker-controlled upstream text |
| 4.2 | 4 | ❌ rejected | **critical**: top-level ``response_format`` in the request body is the Chat-Completions wire shape; the Responses API expects ``text.format`` for ``json_schema`` / ``json_object`` (advertised STRUCTURED_OUTPUT_JSON_SCHEMA capability would have been ignored or rejected by /v1/responses); important: adapter claimed schema support but only validated top-level type + object-root shape; important: structured-output decode failures used ``raise … from exc`` whose ``json.JSONDecodeError.doc`` retains the raw response text (RAW_RESPONSE_LEAK via ``__cause__`` graph); minor: ``str(response_data.get("id") or …)`` accepted arbitrary upstream objects |
| 4.2 | 5 | ❌ rejected | important: minimal schema validation (top-level type + required-field presence) is far short of full JSON Schema (``anyOf`` / ``allOf`` / ``pattern`` / nested types / ``enum`` / bounds / ``additionalProperties``) — should not advertise ``STRUCTURED_OUTPUT_JSON_SCHEMA`` until full validation lands; important: constructor defaulted ``runtime_mode=RuntimeMode.FIXTURE`` and didn't consult ``current_mode()``, so ``VERACRAWL_RUNTIME_MODE=production`` would be silently ignored; important: fixture mode accepted any ``httpx.BaseTransport`` including ``httpx.HTTPTransport`` (real network egress); minor: tests don't assert log-capture or env-based production gating. **All three importants addressed in post-iter-5 commit a834fe6** (``_SUPPORTED_CAPABILITIES = frozenset()``: every flag returns ``False`` until the backing implementation lands; ``runtime_mode: RuntimeMode | None = None`` falls through to ``current_mode()`` when omitted; fixture mode now restricted to ``httpx.MockTransport`` specifically); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol. Full JSON-Schema validation deferred to **Phase 4 step 4.6** ``schema_runtime`` (Pydantic-class round-trip); tool-call support deferred until ``ProviderResponse`` grows a tool-call field; vision / extended-thinking similarly deferred. Real-API live tests deferred to **Phase 6 step 6.5** |
| 4.3 | 1 | ❌ rejected | important: ``_split_system_messages`` hoisted ALL SYSTEM messages regardless of position, silently reordering conversations; important: provider-swap acceptance test missing from the diff; minor: Pydantic ``ValueError`` from ``ProviderResponse`` validators surfaced raw (could leak upstream values via the validator's error message) |
| 4.3 | 2 | ❌ rejected | important: ``parsed_output`` was populated for ``JSON_SCHEMA`` requests with only minimal validation (object root + required fields), violating the contract that ``parsed_output`` means the payload was validated against ``request.response_format.json_schema`` |
| 4.3 | 3 | ❌ rejected | **critical**: ``_extract_output_text`` joined Anthropic text blocks with empty separator (``"hello"`` + ``"world"`` → ``"helloworld"``); important: codex flagged ``parsed_output=None`` on JSON_SCHEMA as a "regression" (contradicting iter-2's settled policy); important: provider-swap test comments mismatched the request shape |
| 4.3 | 4 | ❌ rejected | **critical** (both adapters): JSON_SCHEMA half-measure (accept request, parse text, return ``parsed_output=None``) was an inconsistent middle ground — codex flip-flopped between iter-2 (don't populate) and iter-4 (validate or refuse). Resolution: refuse ``JSON_SCHEMA`` at adapter boundary in both v2 adapters with explicit migration pointer to Phase 4 step 4.6 ``schema_runtime`` |
| 4.3 | 5 | ❌ rejected | important: Anthropic Messages API requires alternating USER/ASSISTANT turns, but provider-blind ``ProviderRequest`` allows any sequence; consecutive same-role messages would fail at the wire boundary; important: codex re-flagged JSON_SCHEMA refusal (held the line per iter-4 settled policy); minor: Anthropic module docstring lagged the iter-4 refusal change; minor: provider-swap test only covered single system+user turn. **Important + minors addressed in post-iter-5 commit 7290d5b** (``_coalesce_same_role_runs`` joins consecutive same-role USER/ASSISTANT messages with ``\\n`` for the alternating-turn requirement; module docstring updated to match the JSON_SCHEMA-refusal + same-role-coalesce semantics; new ``test_provider_swap_multi_turn_request_succeeds_on_both_providers`` covers a 5-message conversation across both adapters); status = DONE_WITH_RESERVATIONS because no formal iter-6 review per the 5-iter cap protocol; JSON_SCHEMA "regression" claim held the iter-4 settled refusal policy and is recorded as a step 4.6 reservation. Tool-call symmetric refusal + multi-turn real-API coverage deferred per "v2 phase 4 step 4.3 reservations" |

## V1 (Sept 2026) — preserved

| ID | Title | Status | Started | Completed | Commits | Notes |
|----|-------|--------|---------|-----------|---------|-------|
| P0-1 | HTTP client (urllib → httpx) | DONE | 2026-05-06 | 2026-05-07 | (this commit) | urllib → httpx；retry/Retry-After；per-hop redirect SSRF (opt-in via config)；real Chrome UA；NetworkAdapterError(ValueError) with failure_type；20 個新 unit test |
| P0-2 | Playwright rendering stability | RETRACTED → DONE (narrowed scope) | 2026-05-07 | 2026-05-07 | 3225002 + (revert commit) | original commit shipped 5 stealth init scripts which violate docs/09:116 §Safety Boundary ("stealth automation" is forbidden); reverted. Final scope = real Chrome UA + locale + Accept-Language for rendering stability under authorized access only. context reuse stays P1. |
| P0-3 | OpenAI adapter fix | DONE (substantively) | 2026-05-06 | 2026-05-07 | 4c9bce1 + (this commit) | RAW_RESPONSE_LEAK + bogus model + urllib→httpx + retry/Retry-After + max_output_tokens raised；structured output schema wiring 列 P1 |
| P0-4 | Tool Gateway gating | IN_PROGRESS (sub-step 1/N) | 2026-05-06 | - | (this commit) | policy-aware allowlist + per-actor quota + audit log；legacy mode 保留向後相容 |
| P0-5 | Structured logging | DONE (substantively) | 2026-05-06 | 2026-05-06 | sub-steps 1-4 + boundary | infrastructure complete; boundary test 強制 internal 不能 raw import logging/structlog；P1 follow-up：CLI 非合約 print 遷移、3 個內部模組 logger demo |
| P0-6 | CI workflow | DONE | 2026-05-06 | 2026-05-06 | (this commit) | ci.yml + nightly.yml + dependabot.yml + live marker registered |
| P0-7 | Break optimization cycle | DONE | 2026-05-06 | 2026-05-06 | (this commit) | 2 types 移至 contracts/optimization_runtime.py；6 個下游 import 改 contracts；boundary test 強制 |
| P0-8 | Runtime mode (prod vs fixture) | DONE | 2026-05-06 | 2026-05-06 | cd02719 + (this commit) | foundation + 4 個 gate 接線完成；9 個 production-mode regression test |

## Status 值

- `NOT_STARTED` — 尚未開始
- `PLAN_REVIEW` — 該 plan 在跑 codex plan review
- `IN_PROGRESS` — 實作中
- `TASK_REVIEW` — 已 commit，在跑 codex task review
- `DONE` — task review 通過
- `BLOCKED` — 卡住（在 Notes 寫原因 + 連續失敗次數）

## 全局狀態

- **目前活躍項**：P0-1（BLOCKED）、P0-5（BLOCKED）
- **連續失敗次數**：P0-1=3、P0-5=3
- **最後一次更新**：2026-05-06

## 共同 Pattern 觀察（兩個 P0 plan 都 3 連敗）

| 維度 | P0-1（HTTP client） | P0-5（structured logging） |
|---|---|---|
| iter 1 | 1 critical + 10 important + 1 minor | 0 critical + 6 important + 3 minor |
| iter 2 | 3 critical + 8 important + 2 minor | 0 critical + 8 important + 3 minor |
| iter 3 | 2 critical + 6 important + 2 minor | 0 critical + 9 important + 3 minor |
| 趨勢 | 議題從契約 / SSRF 收斂到 details | 始終是 details，design 從未被質疑 |
| 關鍵差異 | P0-1 確有 critical 設計問題（SSRF / 失敗傳播）| P0-5 design 健全；codex 在挑寫作精度 |

**觀察**：codex plan review 的標準極高，每輪都會找到「缺漏的覆蓋」、「邊角 case」、「術語誤用」等。短期內難以一次過審。

## 阻塞 / 待人類決策

**P0-1 計畫 codex 連續 3 輪未通過。** 依用戶 CLAUDE.md 規範必須停下來重新評估。

iter 3 主要新問題：
1. **critical**：v3 將所有 retryable 失敗（含 transport timeout）retry 耗盡後映射至 `RETRY_EXHAUSTED`，但 unit test 6/7 期望 `NETWORK_TIMEOUT` → 自相矛盾
2. **critical**：SSRF 防護只覆蓋 redirect target；初始 request URL 未在 adapter 內 re-validate（雖然 acquisition 層有 `network_policy_failure` 檢查，但 adapter 不應假設上游必跑）
3. retry pseudocode 用 `response if 'response' in locals() else None` 會跨 iteration 讀到 stale response
4. `execute_source_acquisition` 改不吞 `NetworkAdapterError` 影響非 HTTP adapter（browser / structured source 等）— 需 call-site 分析
5. failure_report 沒帶 attempt_evidences / redirect_hops 部分結果
6. `RETRY_EXHAUSTED` enum 需在 `_failure_report` 的 operator_status mapping 加對應字串
7. redirect policy 未涵蓋：relative Location、malformed、missing scheme/host、credentialed URL、非 http(s) scheme、跨 redirect 的 header 處理（Authorization 是否帶過去）
8. DNS rebinding test / streaming size-budget test 在純 `httpx.MockTransport` 不可行（需 DNS injection / 自訂 streaming response）

觀察：每輪 codex 都解 12+ 議題但又揭露新層次。問題核心是 P0-1 的範圍太大，跨：
- adapter 層（HTTP client）
- contract 層（NetworkResponse / FailureType / AttemptEvidence）
- ports 層（NetworkClientResult）
- fetch 層（acquisition.py 的失敗傳播）
- test infrastructure（DNS / streaming injection）

**建議用戶選擇**：
- (a) 繼續 iter 4-5（風險：仍可能不過；context 持續累積）
- (b) **將 P0-1 拆為 4 個子項**並各自走 plan review：
  - P0-1a｜HTTP client 替換（urllib → httpx + UA + timeout，不含 SSRF / retry）
  - P0-1b｜Retry / Retry-After 處理
  - P0-1c｜Per-hop SSRF 加固（redirect + initial URL + DNS resolve）
  - P0-1d｜Size budget streaming + evidence
- (c) 跳過 plan review（接受當前 v3 為 working draft），直接進 TDD 實作；codex task review 抓殘留
- (d) 暫停 P0-1，先處理較簡單的 P0（P0-5 logging / P0-6 CI / P0-8 runtime mode），累積 codex review pattern 經驗後再回頭做 P0-1

## P0-5 Sub-steps

P0-5 拆為以下 atomic sub-steps，每個獨立 commit + codex task review：

1. **redaction processor** — ✅ DONE（commits `bd9b249`, `9517030`, `254ed97`, `787eb8f`；codex task review 4 輪後 approved）
   - follow-up minor：`test_processor_replaces_sensitive_primitive_at_depth_cap` 增加 `REDACTED_DEEP in serialized` 斷言
2. **structlog dependency + `logging.py` 核心** — ✅ DONE（一次 commit；用戶決策跳過 codex task review per commit）
3. `bootstrap_cli_logging` context manager + 1 個代表性 CLI 遷移
4. 其他 67 個 CLI entry points 注入 bootstrap
5. 3 個內部模組（stdlib_http / tool_gateway / observability）展示 get_logger 用法
6. 非合約 print 從代表性 CLI 移除
7. boundary tests（限制範圍 import 規則）
8. 30 個 CLI bootstrap AST scan boundary test

## Codex Review 紀錄

| 階段 | 對象 | Iteration | 結果 | 修正方向 |
|------|------|-----------|------|----------|
| plan | p0-1-http-client.md | 1 | ❌ | 1 critical (per-hop SSRF) + 10 important + 1 minor |
| plan | p0-1-http-client.md | 2 | ❌ | 3 critical (failure-prop, allowlist-wiring, 4xx) + 8 important + 2 minor |
| plan | p0-1-http-client.md | 3 | ❌ | 2 critical (retry-semantics 矛盾, initial-URL SSRF) + 6 important + 2 minor — **3 連敗，停止重新評估** |
| plan | p0-5-logging.md | 1 | ❌ | 6 important + 3 minor (no critical)：129 prints 全在 cli/、structlog factory 與 caplog 不容、idempotency、thread contextvar 錯誤聲明、entry-point 缺清單、無 redaction policy、import boundary 設計衝突 |
| plan | p0-5-logging.md | 2 | ❌ | 8 important + 3 minor (still no critical)：BoundLogger 型別矛盾、idempotency level 不真實生效、reset 動 root handlers 影響 caplog、bootstrap 缺 cid AST 檢查、prog binding leakage、runtime entry inventory 不夠具體、CLI scope/commit 訊息語義不清、import boundary 太寬、ANSI test 不可行 |
| plan | p0-5-logging.md | 3 | ❌ | 9 important + 3 minor (still no critical)：propagate=False vs caplog 矛盾、structured fields 不在 record.attr、entry-point 數應為 68 不是 30、AST 檢查太弱、cli_token 邏輯誤、bind_runtime_context 缺設計、結構化 error print 分類不清、correlation 覆蓋與 Why 矛盾、boundary 漏列 11 個套件 — **3 連敗，停 plan review，改走 TDD** |
| task | bd9b249 (P0-5 sub-step 1: redaction processor) | 1 | ❌ | 3 important：top-level only redaction（缺 recursive）、漏 password/private_key/x-api-key/session_id/csrf 等 sensitive key、STATUS.md scope 過大宣稱 |
| task | bd9b249..9517030 fix-up | 2 | ❌ | 1 important：depth cap 是 fail-open，sensitive 值在深層仍會洩漏；應 fail-closed 用 placeholder 取代整個 sub-tree |
| task | 9517030..254ed97 fix-up | 3 | ❌ | 1 important：fail-closed 後仍可能漏 primitive — 簡化為 cap 處全部替換 REDACTED_DEEP（含 primitive） |
| task | 254ed97..787eb8f fix-up | 4 | ✅ | **approved**！只剩 1 minor：建議 regression test 增加 `REDACTED_DEEP in serialized` 斷言（記為 P0-5 sub-step 1 follow-up） |

iter 1 主要問題：
1. **critical**：redirect 只擋 HTTPS→HTTP downgrade，未對 redirect target 重跑 egress / private-network / DNS-rebind policy
2. constructor 不相容當前 13 個 call site（`StdlibHttpSourceAdapter(request)` 是 request-bound，plan 提案 config-only `fetch("url")`）
3. 既有 `NetworkResponse` ref-based contract 無法承載 plan 提的 headers / elapsed / attempt_number 證據；需擴 contract 或走 sidecar artifact
4. `execute_source_acquisition` 只 catch `ValueError`，plan 的 test 期望 raw httpx 例外 → 需 deterministic 例外 → `NetworkFailureType` 映射
5. retry 設計含糊（httpx 不會自 raise 429/5xx；tenacity 不自動讀 Retry-After；max_retries 語意不明）
6. httpx proxy API 用了過時的 `proxies` 而非 0.27+ 的 `proxy=`
7. acceptance 含 P0-2 / P1-5 scope 的 UA grep（real-benchmark / browser-quality）
8. AGENTS.md spec 追溯要求 — 但用戶已拔除 spec-kit
9. 測試用 10.255.255.1 黑洞 IP / `https://x.test` / `MockTransport` 驗 proxy 等不可靠
10. live test 用 httpbin.org/status/429 永遠回 429 無法驗 retry-to-200
11. 寫死 `Wed, 21 Oct 2026 ...` HTTP-date / `Chrome 131` 字面值
12. HTTP/2 motivation 與 dependencies 矛盾

v3 修正策略（iter 2 主要問題）：

iter 2 critical：
1. `execute_source_acquisition` 內部 catch `ValueError` 會吞掉 `NetworkAdapterError` → 必須改 acquisition.py 使其不吞此 type
2. `egress_allowlist` 在 adapter config 預設空集合，但 13 個 call site 用 `StdlibHttpSourceAdapter(request)` 不傳 config → 跨域 redirect 仍未擋 → factory + 接線到 `execute_http_network_acquisition`
3. retry 迴圈把 401/403/404/410/422 當 success 回，但呼叫端假設「any adapter result is success」→ 必須明確決定 4xx 是「成功 HTTP 採集」（status 寫進 NetworkResponse）

iter 2 important：
4. evidence 沒有 artifact_store 注入點 → 改為 inline list（無 store 依賴）
5. raise 時 `last_result` 為 None → 失敗仍 populate 部分結果
6. `RETRY_EXHAUSTED` enum 不存在 + last_failure_type 對 retryable 5xx 未更新
7. `MockTransport` 無注入點 → 加 `transport=` constructor kwarg
8. retry 計時測試會睡 2-60s flaky → 注入 `sleep_fn`、`clock_fn`；`_compute_wait` 變 pure function 直接驗
9. timeout 雙來源（config vs request.timeout_ms）邏輯矛盾 → 規則：config 顯式注入時 canonical；無 config 時用 request.timeout_ms
10. 沒做 size_budget streaming → 用 `httpx.iter_bytes` streaming
11. exception mapping 太粗（ConnectError 全部 → NETWORK_TIMEOUT）→ DNS/refused/TLS/proxy 細分

iter 2 minor：
12. 列 tenacity 但實際自寫 → 移除 tenacity，stdlib random.uniform
13. HTTP/2 motivation 與 scope 矛盾 → 徹底移除

v3 主要新增結構：
- `NetworkClientResult.attempt_evidences: list[NetworkAttemptEvidence]`（inline）
- `NetworkFailureType.RETRY_EXHAUSTED` 新 enum
- `build_http_adapter_for_acquisition` factory 接線 allowlist
- `transport` / `sleep_fn` / `clock_fn` 注入點
- 4xx 設計決策：視為「成功 HTTP 採集」，status 帶出去由上游分類
- exception mapping 表格化
- streaming size budget enforcement
- `_compute_wait` pure function（測試直接驗）

v2 修正策略：
- 保留現有 constructor + execute() 介面，新加 `*, config=None` kwarg
- 自寫 retry loop，不依賴 tenacity decorator；明確 `max_attempts`（含首次）
- per-hop redirect 重跑完整 policy（egress + private + DNS resolve + protocol）
- 證據走 sidecar artifact `NetworkAttemptEvidence`，不破現有 ref-based contract
- 例外全部 `ValueError` 子類附 `failure_type` 屬性，向後相容 catch
- pin `httpx>=0.27,<1.0` + `proxy=`
- acceptance 只驗 P0-1 scope 的 UA
- 用 `pytest-httpserver` 取代 httpbin / 黑洞 IP
- 移除 HTTP/2 自 motivation（列 P1）
- 對齊 `docs/02-production-architecture.md` + `docs/07-data-contracts.md`（spec 追溯改走 docs/，因 spec-kit 已拔）


## 完成清單

P0 全綠後，產出：
- 新增 / 修改 / 刪除檔案統計
- 測試新增數量 + 覆蓋率變化
- 已知未解問題（不要藏）
- 需要人工決策的 open questions
