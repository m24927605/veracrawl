# Reassessment #2 — production-authorized-source-crawler v2

**Trigger**: 累計 8/8 step `DONE_WITH_RESERVATIONS`(觸發停損上限,放寬後第二次)
**Timestamp**: 2026-05-08T09:15:13Z
**Master HEAD**: `e559b29`(Phase 1 step 1.5 完成 + STATUS.md 紀錄)
**剩餘 steps**: 26(Phase 1 step 1.6 ~ Phase 6 step 6.5)
**前次 reassessment**: `reassessment-20260507T181555Z.md`(5/5 → 上限 8 + iter1 6-點 mental scan)

---

## 1. 7 個 reservation 完整列表

| Step | Reservation 摘要 | 後續 phase / 類型 |
|------|-----------------|------------------|
| 0.3 | 運行時 ReDoS hardening | Phase 2 step 2.2 |
| 0.4 | 結構化 `CredentialScopeViolation.reason` | Phase 2 step 2.2 |
| 1.1 | `tests/unit/test_browser_session_storage_state.py` 1000+ 行重構 | code-quality |
| 1.2 | 高並發 DNS resolution shaping | Phase 1 step 1.6 |
| 1.3 | post-iter-5 fix-up: `RateLimitProhibited` contextmanager + malformed-port fallback userinfo | Phase 1 step 1.6 |
| 1.4 | 多項 Phase 6 / step 1.5 follow-up:source-of-truth 對齊、per-attempt 隔離、tracing.start scope、size streaming、constant 重複等 | Phase 6 |
| 1.5 | 多項 follow-up:sensitive-header 共用常數、kw_only、conformance tests、streaming-spool put、ordered cookies、jar production-mode gate | Phase 6 / code-quality |
| (1.5) | 之前 1.4 第 7 個還未數,實際 7 個 + 1.5 = 8 個總計 |  |

修正:其實第 8 個是 step 1.5 的 reservation(本身即 8/8 上限第 8 個)。

---

## 2. 與第一次 reassessment 對比

### 第一次(2026-05-07,觸發於 5/5):
- iter 1-4 平均 reject 數:3 (per step)
- iter 5 仍出 important 概率:**83%**(5/6)
- 採取對策:上限 5 → 8 + iter1 6-點 mental scan

### 第二次(2026-05-08,觸發於 8/8):
- iter 1-4 平均 reject 數:**5+** (per step,顯著上升)
- iter 5 仍出 important 概率:**100%**(3/3 — step 1.3 / 1.4 / 1.5,且都需要 post-iter-5 fix-up)
- step 1.4 + step 1.5 各自 5 輪都各有 6-12 個 important findings,單個 step 可累積 30+ 修正

### 6-點 mental scan 效果評估
- **有效**:iter1 critical 大幅減少(0.3-1.3 都有 critical / iter1;1.4-1.5 沒有 iter1 critical)
- **不足**:iter 2-5 的 important findings 數量反而上升(因為 step 1.4 + 1.5 surface area 大,每輪 codex 都能挖到下一層)

---

## 3. 真實情況分析

### 觀察 A:scope 大小 vs codex 嚴格度
| Step type | iter1-5 average important count | 結局 |
|-----------|------------------------------|------|
| Pure contracts (0.2 / 0.3 / 0.4) | 2-3 / iter | 多輪後接近收斂 |
| Adapter wiring 小範圍 (1.2 / 1.3) | 2-3 / iter | iter 5 仍有 2-3 個 important |
| Adapter + multiple ports (1.4 / 1.5) | **5-8 / iter** | iter 5 仍有 6+ 個 important |

**結論**:codex 的 iteration depth 與 step 的 surface area 成 **超線性**(每多一個 port / 多一個邊界,findings 不只加法,而是乘法)。

### 觀察 B:剩餘 26 step 的 surface 分佈

| Phase / step | 預估 surface | 預估 reservation 風險 |
|--------------|------------|---------------------|
| 1.6 (live tests) | 中 | 中 — 真實網路 + 多項既有 follow-up 整合 |
| 2.1 (CredentialVaultPort) | 中 | 中 — 單 port + 兩個 impl |
| 2.2 (SessionScopePolicy + 0.3/0.4 reservation 落地) | **大** | **高** — 既有 reservation + 新 policy + runtime |
| 2.3 (RedactedPromptContext) | 小 | 小 — 純 redaction |
| 2.4 (AuthorizedSessionAdapter + outbox) | 中 | 中 |
| 2.5 (Agent runtime credential lifecycle + live test) | **大** | **高** — 跨 phase + live |
| 3.x (6 個 step) | 中-大 | 各有風險 |
| 4.x (8 個 step) | **大** | **很高** — LLM provider port v2 + extraction 重寫 |
| 5.x (5 個 step) | 中 | 中 |
| 6.x (5 個 step) | **大** | **很高** — production gate + DR drill + live |

預估剩 26 step 會用掉 12-18 個 reservation。8 上限不夠。

### 觀察 C:codex 標準是 production 等級的
回頭看 step 1.5 iter 5 的 6 個 important — 沒有一個是「挑剔」:
- 失敗路徑不發 partial result → 真實 replay/audit gap
- CookieRecord raw repr → 真實 credential leak vector
- CachedConditional whitespace 驗證 → 真實 contract gap
- 跨 origin parametric coverage → 真實 regression gap

如果這是 production crawler 上線檢查表,這些都會被擋下來。codex 沒有過嚴。

---

## 4. 三個方向對比

### 選項 A:再次放寬上限到 16
- **代價**:接受 ~50% 的 step 會 DONE_WITH_RESERVATIONS
- **風險**:每個 reservation 都是真實的 follow-up,累積到 Phase 6 可能還有 12-15 個未解決 important findings,production gate (Phase 6) 會被卡住
- **節奏**:可繼續推進,每個 step 跑完 5 輪 codex,完成 26 step 預估再 ~30 hr 工作

### 選項 B:大型 step 強制拆分
- 每個有「多個 port + adapter wiring」的 step 拆成 sub-steps:
  - 1.4 應該拆成 1.4a (port + LocalFs impl) / 1.4b (HAR redaction) / 1.4c (Playwright wiring)
  - 1.5 應該拆成 1.5a (per-attempt evidence) / 1.5b (cross-redirect strip) / 1.5c (conditional cache port + impl) / 1.5d (cookie jar port + impl) / 1.5e (wiring + integration)
  - 4.x 應該每個 sub-component 拆 sub-step
- **代價**:總 commit 數翻倍,但每個 step 收斂快(iter1-3 應該就過)
- **節奏**:多 commits 但少 reservation,可能跑得更快也更乾淨
- **問題**:design.md / implementation-prompt.md 都以原 step 為單位,需要重寫進度紀錄結構

### 選項 C:接受所有 1-2-3 importants 為 reservation,但不 post-iter-5 fix-up
- 跑 codex iter 1-5,如果 iter 5 仍有 important,**直接列入 reservation**,不寫 fix-up
- 不再追求逐個解決
- **代價**:reservation 累積快(可能 26 step 用 18-22 個),production gate 可能 200+ 個未解決 important
- **風險**:Phase 6 通過會非常難(需要逆向回頭修)
- **節奏**:最快 — 每個 step 跑 4-5 hr 不多

### 選項 D:選 A 上限放寬 + 強制拆 大 step (combine A + B)
- 上限放寬到 16
- Phase 4 / Phase 6 大 step 拆成 sub-steps  
- 中等 step (1.6 / 2.x / 3.x / 5.x) 不拆
- 預估剩 26 logical step → 拆後變 30-35 step,但 reservation 用量 < 14
- **節奏**:平衡

---

## 5. 我的建議

**選項 D**(放寬到 16 + 拆大 step)。

理由:
1. 純放寬(A)解不了 Phase 4 / Phase 6 那些必然爆掉的 step
2. 純拆分(B)需要重寫 design / prompts,半路改規則 risk 高
3. 跳過 iter 5 fix-up(C)技術債堆到 Phase 6,production gate 過不了
4. (D) 是兩個槓桿一起拉:上限放寬處理「中等 step 仍會有 1-2 個 reservation」的不可避免 long tail;拆分處理「大 step 必爆」的根因

**具體做法**:
- 上限 8 → **16**(剩 26 step,預估 12-18 個 reservation,16 上限留 0-4 緩衝)
- Phase 4 step 4.6 (`schema_runtime.py` 重寫) 拆成:
  - 4.6a: 新 LLM-driven extraction core
  - 4.6b: anchor-grounded citations
  - 4.6c: V1 `processing.ExtractionCandidate` retire + registry rename
- Phase 6 step 6.1 (`OtelObservabilityAdapter`) 不需拆(單 port)
- Phase 6 step 6.4 (live integration tests) 拆成:
  - 6.4a: 設計 7 個 live test scope
  - 6.4b: 跑通各個 live test
- 其他 phase 暫不拆(寫 step 時若發現 surface 太大再決定)
- 接著做的 step 1.6 不拆(scope 已小)

不更動 iter1 6-點 mental scan(它對 critical 防範有用)。

---

## 6. 結論 / 等待用戶指示

請選:
- **(A)** 上限 → 16,繼續直接做 step 1.6
- **(B)** 強制拆分大 step,維持 8 上限,從 step 1.6 開始重新規畫
- **(C)** iter 5 跳過 fix-up,直接列 reservation,維持 8 上限
- **(D)** 上限 → 16 **+** 拆 Phase 4/6 大 step(我傾向)
- **(E)** 你的方向

選定後我會更新 `implementation-prompt.md` / `resume-prompt.md` 對應規範,然後從 step 1.6 開始繼續執行。
