# Contract: Link Provenance And Site Model

- Every extracted link must have `LinkProvenance`.
- Relative URLs must be resolved against the source URL.
- Duplicate links may be retained with distinct anchor refs.
- Page type classification must use generic signals only.
- Site model records are planning inputs, not evidence.
